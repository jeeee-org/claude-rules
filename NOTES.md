# 学び・方法論（NOTES）

> 要件でも進捗でもない、**進め方・設計判断の理由・ハマりどころ・罠**を残す（メタな学び）。
> **いま効く学びだけ**を置く。項目の頭に取得日を書く（`- <YYYY-MM-DD> …`）。
> 似たトピックで開いた項目をその場で見直し、ルール・スキル・テストに昇格したら移した先を
> 1行残して消す／前提が変わって効かなくなったら消す／一回限りのログはcheckpoints/へ。

最終更新: 2026-09-20

## ルール本文の編集
- 2026-09-12 全体圧縮のやり方：ルールの数と見出し番号は動かさず、「他所で既に言っている語・言い換え・前置き」だけを削る。削った行を1行ずつ見直し、意味が薄れる箇所は戻す（今回は「PJルートで」「原則」「セッション終了時」の3箇所）。新ファイル自身が§9の半角スペース規則を破っていないかは`tools/fix-spacing.py`で機械確認する。
- 2026-09-12 ルールを1本足す時は、同じ節の重複を先に畳むと差し引きで残量が増えることがある（§9追加時・NOTES.md片付け追加時の2回）。追記より先に畳む場所を探す。
- 2026-09-19 境目の半角スペース（§9）の落とし方は`tools/fix-spacing.py`とテストへ移した（行頭のマーカー・日付・章番号・コードフェンス・コード印の透かし方は、すべてテストが守る）。**道具に残らない判断は2つ**——記法を列挙している行（空白が項目の区切り）と、日本語のうしろの`(`で始まる英語の補足。道具は直さずに出すので書き手が決める。
- 2026-09-19 **検出と修正を同じ判定で書くと、判定の穴はどちらにも同じ形で残る。**境目をgrepの文字クラス（左が`` [A-Za-z0-9`)）%] ``）で探していたので、左が`**`で終わる`）** へ`を2回の掃除で見逃していた（道具化して印を透かした初回に出た）。**掃除が終わった文書は、次の道具の実物のテストになる**——道具を作ったら、手で済ませた分に当て直して取りこぼしを見る。
- 2026-09-02 ルールを載せている文書自身がその規則を破っていると効かない（境目の半角スペースが`global-rules.md`に190か所あった）。新ルールは正本自身にまず当てる。

## 記録の軸
- 2026-09-12 ADRを独立した節にしない理由：ADRの4要素（背景・決定・理由・影響）は「決めたこと／進み具合」の軸を切った時点で置き場が決まっている（決定＝方針→`REQUIREMENTS.md`、理由→`NOTES.md`の「設計判断の理由」、経緯→checkpoint）。節を残すと同じ決定が方針とADRの2箇所に載り、置き分けの基準が説明できない。`PROGRESS.md`は「いまの状態」の場所で、決定の一覧は属さない。
- 2026-09-17 公開面から値を落とす時、「前 → 後」の対応表を記録に残すと、**記録自体が元の値を公開する**。落とした**種類**と置き換え後の書き方だけを残す。何を消したかの説明は種類で足りる。
- 2026-09-17 公開リポではforce pushだけでは消えない（実測：履歴を1コミットへ畳んだ直後も、旧コミットはハッシュ指定でAPIから本文が返った）。**リポを削除して作り直して初めて404になる**。GCの依頼でも消せるが時期は保証されない。**だから値を公開面に出さない方が先**で、消す作業は後始末にすぎない。
- 2026-09-12 README「経緯」を据え置いた理由：過去の経緯は事実なので書き換えない（このリポの流儀）。以後をcheckpointへ切り替えれば二重管理にならない。

## 移行ツールの設計
- 2026-09-15 「実パスで判断する処理」と「文字列で置換する処理」を同じ本文に続けてかけると、前者が「触らない」と決めた箇所を後者が壊す（`rewrite_links`のあとの`rewrite_text`が、リンク先を日付一致で書き換えた）。置換は判断済みの範囲を飛ばすか、判断と同じ解決規則（書かれた場所から上へ辿る）で行う。同じ名前で別の場所を指す参照は、モノレポでは同じリポの中にある。
- 2026-09-17 移行は「置き場を移す」と「名前を付ける」の2つの作業で、**前者だけ済んだ中間状態が実際に生まれる**（一括で移すと置き場は揃うが、命名は1件ずつ見出しを読む判断なので後回しになる）。入口を移動に固定した道具は、その状態のPJに使えない。**工程が2つある作業の道具は、途中の状態からも入れるようにする。**
- 2026-09-17 手で動かした分は、道具の直しを素通りする。外向きの相対リンクの張り直しは`apply`に入っていたのに、手で移したPJでは効かず2週間気づかれなかった。**直す機能と同じだけ、検査（`check`）が要る**——検査だけが手作業の取りこぼしを拾える網になる。
- 2026-09-18 **スコープ指定は「何を対象に選ぶか」であって「どこまで触るか」ではない。**`--repo`で1つのPJを指しても、そのPJを指す参照はリポ全体にあるので書き換えはPJの外へ出る（それが正しい）。道具が対象の外を触るなら、**触った分を必ず一覧で出す**——利用者はスコープの内側だけを見て終いにする。
- 2026-09-18 gitの`REV:PATH`は**リポのルートから**解決する（`./`か`../`で始まる時だけカレント相対）。サブディレクトリで走らせる道具がこれをそのまま使うと、ルート直下の同名ファイルを黙って読む。**比較や検査の「何と比べたか」は常に出す**——取り違えは結果を疑うまで気づけない。
- 2026-09-15 `--repo`のようなスコープ指定は、全サブコマンドで同じ意味にする。planが従いapplyが従わないと、計画は正しく立つのに適用だけ「対象が無い」で落ち、原因が見えない。

## 関門・検査の設計
- 2026-09-18 守らせたい決まりを、私自身への指示だけで担保しない。忘れた時は「忘れたこと」も忘れるので、外から見えない。**判定は自己申告から独立した機械に置く**（2026-09-15のトリアージ分類フックと同じ結論に、記録の抜けで再び行き着いた）。
- 2026-09-18 関門は**禁止ではなく、意識した判断の強制**にする。要らない場面は必ずあるので、止めたうえで「理由を述べて通す口」を用意し、**通した事実がコマンドや記録に残る形**にする。そうすると、抜けたのか意図して省いたのかが後から区別できる——区別できないことが元の問題だった。
- 2026-09-18 **コマンドの文字列を読んで判定する関門は、その道具自身と同じ優先順位で解く。** 自分だけの簡略な解釈（先頭の`cd`だけ見る）は、**決まりに従っているPJほど外す**——`git -C <絶対パス>`を必須にしたPJのworktree運用で、本体cloneの状態で判定していた。解けない指定に出会ったら、推測せず判定を降りる（fail-open）。
- 2026-09-18 **判定は、情報が揃う時点まで遅らせる。**commitの瞬間は、実行前だと書き込みが見えず・実行後だと止められない、という**判定に必要な情報が揃わない唯一の時点**だった。pushの直前ならcommitは既にあり、コマンドの書き方に依存せず正確に判定でき、しかもまだ止められる。**「その操作を止めたい」と「その操作の時点で判定する」は別の話**で、後ろにもう1つ関門があるなら、前の関門は早く気づくための助けでよい。
- 2026-09-18 **例外は、コマンドではなく成果物に残す。** 実行時の指定（環境変数）は実行が終われば消えるので、後の関門からは見えず、数えることもできない。コミットメッセージのトレーラに理由を書く形にすると、**例外が履歴の一部になり、後からいくつ・なぜ通したかを読める**。
- 2026-09-18 **関門が効くかどうかは「コマンドの書き方」で変わる。** 発火が割れるのをセッション単位・リポ単位と2度読み違えたが、実際は**1回の呼び出しに何を詰めたか**だった（ヒアドキュメント以降の切り落としで検出が漏れる／実行前なので書き込みがまだ無い）。**観察の条件を固定せずに原因を名指しすると、他人の検証を誤った方向へ向ける。**割れる要因を1つずつ固定して打ち直す。
- 2026-09-18 **実行前に判定する関門は、「前の呼び出しまでに変更ができていて、この呼び出しはcommitだけ」の形でしか判定できない。** 同じ呼び出しで書いてからcommitする形は、書き込みがまだ起きていないので素通りする。**判定できない形は通さず、分けて打つことを求める**か、**実行後に履歴そのものを見る網**（止められないが見逃しは残らない）を併せる。
- 2026-09-18 **止めた時はコマンド全体が実行されない。** 「commitだけが止まった」と読むと、同じ呼び出しでしていた`git add`も走っておらず、次のcommitが`no changes`で空振りする。差し戻しの文面で毎回そう言う。
- 2026-09-18 **登録＝有効ではない。効いていない関門は、無い関門より危ない**——あるつもりで省くため。スクリプトを直接叩いた結果は「判定が正しい」ことしか示さない。**呼ばれているかは、実際に使う経路で確かめる**（しかも確認そのものを、素通りする形で打たない）。
- 2026-09-18 **疎通の確認は、判定を通さずに「呼ばれたか」だけを見る形にする。** 判定を通す形（記録なしのcommitを実際に試す）だと、止まらなかった時に「呼ばれていない」のか「判定が通した」のかが混ざる。印を見たら状態にかかわらず必ず止める口を作り、**副作用の無いコマンド**（`--dry-run`）に付けて打てば、本番のリポでも安全に確かめられる。
- 2026-09-18 **仕組みを操る文字列（通す指定・疎通確認の印）は、コマンドの本文と区別して探す。** コマンド全体から探すと、**その仕組みを説明する文書を書いた回に効いてしまう**。止まる側の誤作動はすぐ気づくが、**外れる側は黙って素通りする**ので危ない（「通すにはこう書く」と記録に書いた回だけ、関門が外れる）。判定の対象はヒアドキュメントより前だけにする。
- 2026-09-18 **自分に効く関門は、直す手順まで自分に塞ぐことがある。** 印を含むコマンドが打てなくなり、パッチをファイルに書いてから実行した。逃げ道（別の入口・印を含まない経路）を1つ残しておく。
- 2026-09-20 **誤検知は「止まりすぎ」では終わらない。逃げ道を作り、その逃げ道が関門の穴になる。**記録の関門がcommitメッセージ中の`<…@…>`をリダイレクトと誤認して止めたので、止められた側は`-m`をやめて`-Fファイル`へ回り、**そのcommitは判定を素通りした**。関門の空振りを直す優先度は、止まる側の不便ではなく**外れる側の穴**で決める。
- 2026-09-20 **規約は「どちらが勝つか」まで書かないと、渡された指示に負ける。** §5.2は「業務/共有リポではAI署名を付けない」とだけ書いており、セッション側から「付けよ」と渡された時にどちらへ従うかを私の判断に委ねていた（実際に負けて共有ブランチへ押した）。同趣旨をPJの`CLAUDE.md`に**明示していたリポでは勝てていた**。**規約は、競合する指示が来る前提で優先順位ごと書く。**そのうえで機械の歯止めを置く。
- 2026-09-20 **例外の置き場は、その関門が何を見ているかで決まる。** 記録の関門はコミットメッセージのトレーラ（`記録なし: <理由>`）で通せるが、AI帰属行の関門は**メッセージそのものを見ている**ので、そこに例外を置くと堂々巡りになる。コマンド側の指定しか残らない。
- 2026-09-18 `git status --porcelain`は**未追跡のディレクトリを`dir/`の1行に畳む**。配下のファイル名で判定する検査は、新しく作ったディレクトリの中身を丸ごと取りこぼす（モノレポで`tasks/a/checkpoints/…`を見落とした）。`-uall`を付けて1ファイルずつ出す。
- 2026-09-21 `git status --porcelain`（-z無し）は**空白を含むパスを`"…"`で囲む**。`core.quotepath=false`は非ASCIIの引用を止めるだけで、空白の引用は止まらない。行頭が`"`になり`(^|/)`の正規表現から漏れる。`-z`で読む（改名は元の名前が次の要素で来るので読み捨てる）。`ls-files`と`show --name-only`は空白を引用しない（実測）

## 配布の仕組み
- 2026-08-29 常時ロードのルールにツール名を書くと、そのツールを入れていないPCで指す先の無い手順が残る（cadenceの事故）。動作で書く。quorumが注入するトリアージ節に共通ルールを足すのも同じ理由で避ける。
- 2026-08-19 見出し番号を詰めると、各PJの`CLAUDE.md`からの`§5.1`等の参照が黙って壊れる。番号は据え置く。
- 2026-09-20 ClaudeのAGENTS.md探索は、作業dirと**その祖先すべて**の`<dir>/AGENTS.md`と`<dir>/.claude/AGENTS.md`を両方読む（実測）。`~/.claude/AGENTS.md`も`$HOME`配下で作業する限り射程に入るが、`$HOME`の外（WSLの`/mnt/c`等）へ出ると黙って落ちる。
- 2026-09-20 PJに`CLAUDE.md`があると既定でAGENTS.mdは**全部**読まれない（実測。部分的に残らない）。`~/.claude/CLAUDE.md`はこの抑止判定に数えられない。`claude-md-and-agents-md`にすると両方載るが、設定は`~/.claude/settings.json`・`--settings`・managedのみで**project/local settingsでは効かない**。グローバルCLAUDE.mdをやめて内容をPJへ降ろす案は、PJ側も`AGENTS.md`1本にしない限り成立しない。
- 2026-09-20 Codexの`AGENTS.md`探索は**git rootで止まる**（openai/codex issue #15683）。共通の親ディレクトリに1枚置いて複数リポで共有する形は、Claudeでは成立するがCodexでは成立しない。層を揃える時はここが非対称。
- 2026-09-26 共通ルールを1枚にした時、文体はClaude側に寄せた（常時ロードで毎回読むのはClaude側が主で、圧縮と§9の掃除も済んでいる）。Codex側だけにあった文（§6の取得日の書き方・§9の理由の言い直し・§4の対応表）は、Claude側で2026-09-12に意図して落とした物と同じなので戻さなかった。§7だけはPJ側に書く物の例示を全版へ足した（PJへ書き込む版には`init-rules`が無く、雛形の代わりになる物が要る）。
- 2026-09-26 PJへ書き込む版の`both`は、PJのルールを`AGENTS.md`に統一し、`CLAUDE.md`は作らない（Claude Codeはv2.1.277以降`AGENTS.md`を直接読む）。`CLAUDE.md`が既にある時だけ先頭に`@AGENTS.md`を足す——あるとClaude Codeが`AGENTS.md`を読まなくなるため。当初は常に`CLAUDE.md`を作って繋いでいたが、統一の方針とずれるので改めた。ブロックはファイルの**先頭**に置く（下に続くPJ固有の指示が「このブロックの外が優先」と読める並びにするため）。
- 2026-09-26 個人の運用は、選ばなかった時に「代わりの決まり」が要るもの（commit・push）と、決まりごと消えてよいもの（worktree・ツール名）とで分けた。自動pushを外して何も書かないと、AIは既定の振る舞いに戻り、PJごとに解釈が割れるため。初回に選択を省くと道具は止まる（黙って既定を入れると、選ばせる意味が無い）。
- 2026-09-25 `install.sh`は`set -e`なので、`x=$(jq …)`のjqが失敗すると**何も言わずにスクリプト全体が途中で終わる**（表示の設定を足す段で踏んだ。後続の配置も走らなかった）。設定を書き換える段は`|| { 警告; return 0; }`で受ける。jqの`with_entries(select($e | has(.key)))`は`.key`が`$e`に対して評価されてnullになる——`.key as $k | $e | has($k)`と書く。

## ループのひな型
- 2026-09-25 Opus 5.5（とOpus 4.8以降・Sonnet 5以降）ではClaude Codeのto-doツール（TaskCreate等）が**既定で外れている**。チェックリスト（`Ctrl+T`）が空のままになる。戻すなら`CLAUDE_CODE_ENABLE_TODO_TOOLS=1`。全体でなくリポ単位で入れるのは、ツールの説明と催促が毎回コンテキストを食い、段取りの表示が要らない作業のほうが多いため。ループの作業表をto-doツールに頼らず`state.json`＋`loopctl.py`で持ったのはこのため。
- 2026-09-25 `claude --agent <名前>`で起動したセッションでは、Stopフックの入力に`agent_type`が入る（実測）。これで早止まりの催促を**統括役のセッションだけ**に効かせ、同じリポの普通の対話には効かせずに済む（公式は人が付き添う使い方に催促を入れるなと言う）。
- 2026-09-25 ワークフロー（`.claude/workflows/`のJS）は**スクリプトから直接シェルを叩けず、実行中に人の入力も受けない**。決定論ゲートをワークフローの中に置くとエージェント経由になり、機械判定でなくなる。統括をメインセッション型にした理由。
- 2026-09-25 `/goal`の評価役はツールを呼ばず、会話に出たものだけで判定する。完了条件は「会話に結果が出る形」（コマンドの終了コードなど）で書く。Stopフックの連続ブロックはClaude Code側で8回が上限。
- 2026-09-25 Jev（TypeSafe AI）は「型付きの問い→確率付きの答え」を返す判断専用モデルで、確率が当たる率と一致するよう学習されている（RLCD）。判断役はこの形（選択肢＋確信度）だけを真似し、学習は手元の記録と較正（閾値と誤りの例）で行う。重みの学習はしない。
- 2026-09-25 ループの止まり方のテストは、**複数工程の一本道の実行で**書く。`--only`で1工程に絞ると「止まった工程の後ろが未着手として催促され続ける」経路を通らず、テストは全部通るのに実際は3回で落ちた（別PCの実走で発覚）。ゲートの出力も、共通部品の締めの行が最後に来るので「最後の行＝理由」は常に定型文になっていた。
- 2026-09-25 cadence（以前の全自動化）が行き詰まった芯は「足す仕組みはあるが減らす仕組みが無い」こと。ゲートが書類（証跡の有無）の検査へ膨らみ、別PCの業務パイプラインでは生きた作業の約3分の2が仕組み自身の保守になった。加えて遷移・ゲート実行・状態の記録がモデルの自己申告だった。ループのひな型では、状態の遷移を`loopctl.py`が強制し、検査の打率と外す候補・起票しない・実行全体の上限・ループ自身の改修禁止を持たせた。
