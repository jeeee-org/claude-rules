# 学び・方法論（NOTES）

> 要件でも進捗でもない、**進め方・設計判断の理由・ハマりどころ・罠**を残す（メタな学び）。
> **いま効く学びだけ**を置く。項目の頭に取得日を書く（`- <YYYY-MM-DD> …`）。
> 似たトピックで開いた項目をその場で見直し、ルール・スキル・テストに昇格したら移した先を
> 1行残して消す／前提が変わって効かなくなったら消す／一回限りのログは checkpoints/ へ。

最終更新: 2026-09-15

## ルール本文の編集
- 2026-09-12 全体圧縮のやり方：ルールの数と見出し番号は動かさず、「他所で既に言っている語・言い換え・前置き」だけを削る。削った行を1行ずつ見直し、意味が薄れる箇所は戻す（今回は「PJルートで」「原則」「セッション終了時」の3箇所）。新ファイル自身が§9の半角スペース規則を破っていないかは`grep -P`で機械確認する。
- 2026-09-12 ルールを1本足す時は、同じ節の重複を先に畳むと差し引きで残量が増えることがある（§9追加時・NOTES.md片付け追加時の2回）。追記より先に畳む場所を探す。
- 2026-09-02 ルールを載せている文書自身がその規則を破っていると効かない（境目の半角スペースが`global-rules.md`に190か所あった）。新ルールは正本自身にまず当てる。

## 記録の軸
- 2026-09-12 ADRを独立した節にしない理由：ADRの4要素（背景・決定・理由・影響）は「決めたこと／進み具合」の軸を切った時点で置き場が決まっている（決定＝方針→`REQUIREMENTS.md`、理由→`NOTES.md`の「設計判断の理由」、経緯→checkpoint）。節を残すと同じ決定が方針とADRの2箇所に載り、置き分けの基準が説明できない。`PROGRESS.md`は「いまの状態」の場所で、決定の一覧は属さない。
- 2026-09-17 公開面から値を落とす時、「前 → 後」の対応表を記録に残すと、**記録自体が元の値を公開する**。落とした**種類**と置き換え後の書き方だけを残す。何を消したかの説明は種類で足りる。
- 2026-09-17 公開リポではforce pushだけでは消えない（実測：履歴を1コミットへ畳んだ直後も、旧コミットはハッシュ指定でAPIから本文が返った）。**リポを削除して作り直して初めて404になる**。GCの依頼でも消せるが時期は保証されない。**だから値を公開面に出さない方が先**で、消す作業は後始末にすぎない。
- 2026-09-12 README「経緯」を据え置いた理由：過去の経緯は事実なので書き換えない（このリポの流儀）。以後をcheckpointへ切り替えれば二重管理にならない。

## 移行ツールの設計
- 2026-09-15 「実パスで判断する処理」と「文字列で置換する処理」を同じ本文に続けてかけると、前者が「触らない」と決めた箇所を後者が壊す（`rewrite_links`のあとの`rewrite_text`が、リンク先を日付一致で書き換えた）。置換は判断済みの範囲を飛ばすか、判断と同じ解決規則（書かれた場所から上へ辿る）で行う。同じ名前で別の場所を指す参照は、モノレポでは同じリポの中にある。
- 2026-09-17 移行は「置き場を移す」と「名前を付ける」の2つの作業で、**前者だけ済んだ中間状態が実際に生まれる**（一括で移すと置き場は揃うが、命名は1件ずつ見出しを読む判断なので後回しになる）。入口を移動に固定した道具は、その状態のPJに使えない。**工程が2つある作業の道具は、途中の状態からも入れるようにする。**
- 2026-09-17 手で動かした分は、道具の直しを素通りする。外向きの相対リンクの張り直しは`apply`に入っていたのに、手で移したPJでは効かず2週間気づかれなかった。**直す機能と同じだけ、検査（`check`）が要る**——検査だけが手作業の取りこぼしを拾える網になる。
- 2026-09-15 `--repo`のようなスコープ指定は、全サブコマンドで同じ意味にする。planが従いapplyが従わないと、計画は正しく立つのに適用だけ「対象が無い」で落ち、原因が見えない。

## 配布の仕組み
- 2026-08-29 常時ロードのルールにツール名を書くと、そのツールを入れていないPCで指す先の無い手順が残る（cadenceの事故）。動作で書く。quorumが注入するトリアージ節に共通ルールを足すのも同じ理由で避ける。
- 2026-08-19 見出し番号を詰めると、各PJの`CLAUDE.md`からの`§5.1`等の参照が黙って壊れる。番号は据え置く。
