# claude-rules改善案メモ

グローバル共通ルール（`rules/global-rules.md` / `rules/codex-global-rules.md`）と`init-rules`スキル、配布の仕組み（`install.sh` / `tools/`）を**使いながら気づいた改善ネタ**を溜める場所。

このファイルが**正本**。`install.sh`がClaude / Codex両方のスキル配置先（`skills/init-rules/IMPROVEMENTS.md`）からここへのsymlinkを張るので、実行時の追記はそのままgit管理下のこのファイルへ書き込まれ、再インストール（`rm -rf` → `cp -R`）でも消えない。溜まったら`git add IMPROVEMENTS.md && git commit && git push`で共有し、他PCは`git pull`で受け取る。

**正本（ルール本体）を編集するPCは1台に限る**（README「ルールを変更するとき」）。他PCで気づいたことは**このファイルへ書き足すところまで**にして、`rules/*.md`は触らない。

- 1件 = 日付＋状況（どのPJ・どの作業で気づいたか）＋気づき（ルール／配布の仕組みのどこが弱いか）＋できれば改善案（具体的な文面や口の形まで）。
- 単発のタスク状況や特定PJのドメイン知見はここに書かない（前者はPROGRESS.md / checkpoint、後者はPJの`NOTES.md`へ）。**全PJに効く共通ルールとしての改善ネタだけ**を残す。
- **このファイルは公開リポに入る。** 業務PJの内部ファイル名・スクリプト名・ツール名・規約の具体値（関門の本数・上限のバイト値・社内PCの制約）と、個人の機材名やIPは書かない。**動作で書く**——「`<内部スクリプト名>`で検査」ではなく「公開手順の機械検査で捕まえる」。抽象化しても気づきの中身は残る（2026-09-17に既存項目をこの形へ直した）。
- 同種の気づきは新規行を量産せず既存項目に統合する（再発頻度・追加事例を足す）。
- 反映したら該当項目の末尾に`⇒ [反映済み → <file> §<節>]`を付ける（二重反映防止）。印の無い項目だけが生きている候補。
- **並びは古い順。新しい項目はファイル末尾へ追記する**（先頭へ差し込まない）。複数PCのcloneから末尾追記どうしが素直にmergeできる形にするため——並べ替えは全項目conflictを起こし、その回の実質的な追記を巻き込んで失う。

---

<!-- 追記はファイル末尾へ。並びは古い順（先頭へ差し込まない）。 -->

## 2026-08-15 — §5.1の終い方に「元cloneの最新化」が無い
- **状況**: 別PC（業務・共有リポ群）で、サブタスクの終わりと起票のときに本体cloneの最新化が漏れていた。実際に本体cloneが3リポで遅れていた（5 / 6 / 4 commit）。
- **気づき**: §5.1が「①元cloneを直接編集しない ②develop / main上で直接編集しない ③終了後に`git worktree remove`まで行う」で終わっており、**元cloneを最新化する手順が無い**。読み取りは元cloneで行う運用なので、古い状態のまま調査を始めてしまう。PJ側では`pull --ff-only`を足して塞いでいたが、**PJで塞いだ穴がグローバルへ昇格していなかった**。症状が出るまでの間が長く、気づけるのは何かがすれ違った後になる。
- **改善案**: ③を「終了後に`git worktree remove`と元cloneの`git pull --ff-only`まで行う——飛ばすと次に読む内容が古い」へ。Codex版（`rules/codex-global-rules.md`）にも同じ穴がある。
- **反映（2026-08-19）**: Claude / Codex両方へ反映。⇒ [反映済み → rules/global-rules.md §5.1・rules/codex-global-rules.md §5]
- **補足（この件で踏んだ運用ミス）**: 生成物である`~/.claude/CLAUDE.md`を直接編集してinstallで消した。正本と生成物の関係が編集する側から見えにくい。⇒ [反映済み → マーカー行が`手動編集しない — 変更はリポのrules/global-rules.mdへ`を持っており充足済み（2026-08-29確認）]

## 2026-08-15 — Codex版の配置を任意化してほしい（メインエージェントに使わないPCがある）
- **状況**: 別PCでCodexをメインエージェントに据える運用をやめた。不要になったのは`$CODEX_HOME/AGENTS.md`（codex-rulesブロック）と`skills/init-rules`・`skills/triage`・`hooks/codex-triage`。**Codex CLI本体は残す**——Claude側quorumのパネリストとして現役のため。
- **気づき**: `install.sh`はCodex側を必ず配置する作りで、配置しない口が無い。消しても次のinstallで戻るため、呼び出し側に**後から消す後処理**を書く羽目になった。「Codexをメインエージェントとして使う」と「Codex CLIを別用途で呼ぶ」は独立なのに、installがその2つを分けていない。
- **改善案**: `CLAUDE_RULES_INSTALL_CODEX=0`（または`--no-codex`）でスキップできるとよい。既定は現状どおり配置でよい。**同じ要望をquorum / cadenceにも出している**（3ツールとも`AGENTS.md`へ注入するため、1つだけ止めても残りが作り直す）。
- **反映（2026-08-19）**: 3ツールとも対応。呼び出し側は「置かせてから消す後処理」を捨てられた——削除対象を列挙する方式はinstall側が置くものが増えるたびに追随が要り、実際に`check-limits.sh`を1件取りこぼしていた。⇒ [反映済み → install.sh（`--no-codex` / `CLAUDE_RULES_INSTALL_CODEX=0`）]

## 2026-08-18 — 上限の単位が未定義・機械検知が片側だけ・グローバル自身に削り代がある
- **状況**: 別PCで常時ロードされるCLAUDE.md 3枚とPROGRESS.mdを点検した。3枚とも上限内だが**残りは8〜20Bしかなかった**。次に1ルール足せば必ず溢れる状態が、誰にも気づかれずに続いていた。
- **気づき①（単位が未定義）**: §2の上限が「14KB / 6KB」表記で、**KBが1024か1000か書いていない**。1000なら3枚とも超過、1024なら全部収まるという逆転が起きる。ルール文からは読めず、過去の記録を掘って初めて1024と確定した。
- **気づき②（機械検知が片側だけ）**: 実際に自動で効いていたのはPROGRESS.mdの行数だけで、**CLAUDE.mdのバイト数を見る仕組みが無い**。PROGRESS.md側も総バイトを見ていないため、行数を守ったまま太れる（実例: 56行で21,062B ＝ 1行平均376B）。「60行」だけでは**1行に詰め込む方向へ逃げるだけ**。
- **気づき③（グローバル自身に削り代がある）**: §4は§1の表の言い換え、§7は`init-rules` SKILLと重複。この2か所で約790B空く。PJ側は既に絞り切っていて経緯削除では250〜350Bが限界だった。
- **反映（2026-08-19）**: ①②③とも反映。§2にバイト数を併記し、判定は`tools/check-limits.sh`に集約（PJ上書きは`CR_LIMIT_*`か`.claude/limits.env`）。§4 / §7を圧縮してグローバルは14,325 → 13,468B。**この判定でPROGRESS.mdの総バイト超過が初めて可視化された**（59行で通っていたが22,875B / 12,288B）。⇒ [反映済み → rules/global-rules.md §2・tools/check-limits.sh]

## 2026-08-27 — cadenceをやめたPCで、global-rulesが`/cadence`を指したまま残る
- **状況**: 別PCでcadenceを外した（ブレが大きく、必要なときに起動せず必要でないときに起動する。実行痕跡はゼロだった）。`~/.claude/skills/cadence`とCLAUDE.mdのcadence-triageブロックも外した。
- **気づき**: §2が`/cadence optimize-context`を名指ししているので、cadenceを入れていないPCでは**指す先の無い手順が常時ロードのルールに残る**。`~/.claude/CLAUDE.md`は毎セッション全文が読まれる面なので、存在しないコマンドを指す行の害が大きい（読む側は毎回「これは何か」から確かめることになる）。
- **改善案**: 上限超過の対処からツール名を落として動作で書く。理由＝**上限の守り方は道具に依存しない**。§5.2の禁止①の例外リスト（quorum / cadence / claude-rules）は名前の列挙で、ツールを持たないPCでも情報として成立するのでそのままでよい。
- **反映（2026-08-29）**: §2を`"cut bytes, not meaning" —— 削るのはバイトであって意味ではない。畳める記録はcheckpointへ逃がす`に置換。`tools/check-limits.sh`の超過メッセージにも同じ名指しが残っていたので併せて直した。§5.2は判断どおり据え置き。⇒ [反映済み → rules/global-rules.md §2・tools/check-limits.sh]

## 2026-08-29 — 逐一報告の決まりが「長い作業」「サブタスク」に限定されている
- **状況**: 別PCの自動テスト基盤のタスクで、利用者から「毎回の応答で今何を進めているかを、サブタスクかサブタスク以外の細かい指示で書いてほしい」と指示を受けた。当時の決まりは「長い作業では」と条件が付き、形も`いま進めているサブタスク = <名前>（<slug>）`でサブタスク前提。**サブタスクを起票しない細かい指示のときに書く形が無い**。
- **気づき**: 利用者が見たいのは「いま何をしているか」であって、それがサブタスクかどうかではない。「長い作業では」という条件も**長いかどうかを書き手が判断することになり、結果として短い作業では何も出なくなる**（実際、その日は指示を10件以上受けて1行を出したのは数回）。
- **改善案**: 条件を外し、形を`いま進めているもの = <何をしているか>。<いまの段>。`へ広げる。理由＝利用者は同時に複数の作業を見ており、ファイルに書いた進行中の印はチャットからは見えない。
- **反映（2026-08-29）**: `## 9. 応答の書き方`を新設し、Claude / Codex両方へ入れた。⇒ [反映済み → rules/global-rules.md §9・rules/codex-global-rules.md §9]

## 2026-08-29 — 正本を、配布先のPCから直接編集していた（運用違反・2件）
- **状況**: 別PCのcloneから`rules/global-rules.md`を直接編集したcommitが2本あった（説明で記号を省略しない決まり／逐一報告の決まり）。運用は「気づきを改善メモに書く → claude-rulesを持つPCで反映 → 配布先はpullするだけ」。
- **気づき**: 改善メモ冒頭の「正本はここ自身なので、直せるものはその場で直す」を、**正本を直接編集してよいと読んだ**のが原因と思われる。実際には「正本の置き場が外部リポから移った」という意味で、**編集するPCまで移したわけではない**。
- **改善案**: ①冒頭の文言を、編集するPCを明示した書き方へ変える ②2本の内容を突き合わせて、必要なら正本へ入れ直す。
- **反映（2026-08-29）**: ①README「ルールを変更するとき」の冒頭に「編集するPCはここに限る／他PCは改善メモに書き足すところまで」を明記し、本ファイル冒頭にも同じ趣旨を置いた。②逐一報告の分は上の項目で反映済み。**「説明で記号を省略しない」の分は未反映**——そのcloneがこのPCに無く本文を取れないため。⇒ [一部反映 → README §ルールを変更するとき]
- **反映（2026-08-29・追記）**: 本文を配布先のgit履歴から受け取り、§9へ入れた（下の「説明で記号を使って省略しない」の項目）。cadenceの後片付けで1,022B空いたため、削る場所を探す必要はなかった。⇒ [反映済み → rules/global-rules.md §9・rules/codex-global-rules.md §9]

## 2026-08-29 — claude-rulesだけIMPROVEMENTS.mdを持っていなかった
- **状況**: 配布先PCから「上流にIMPROVEMENTSが無い」と指摘された。quorum / cadenceはリポrootに`IMPROVEMENTS.md`を持ち、install先のスキルディレクトリからsymlinkを張って**実行時の追記がgit管理下の正本へ届く**形になっているが、claude-rulesだけ無かった（cadenceの2026-08-27の項目にも「claude-rulesはsymlinkを張らずIMPROVEMENTS.mdも持たないため対象外」と書かれている）。
- **気づき**: そのためclaude-rulesへの改善ネタは配布先PCのローカル文書に溜まり、**上流へは会話でのコピペでしか渡らない**。渡ったかどうかも上流側の記録（READMEの経緯）にしか残らず、配布先からは「何が反映済みで何が生きている候補か」が見えない。本ファイルはその穴を塞ぐもの。⇒ [反映済み → IMPROVEMENTS.md（本ファイル）・install.sh（symlinkとstale/dangling検出）・README]

## 2026-08-29 — 上流の記述がcadence撤去に追随していない（配布先とのドリフト）
- **状況**: 配布先PCとの突き合わせで判明。上流の`CLAUDE.md`は「`~/.claude/CLAUDE.md`はclaude-rules / quorum / cadenceの**3ブロック**合計で14,336B」のままだった。配布先は2026-08-27にcadenceを外しており、そちらの記述のほうが新しかった。
- **気づき**: **ツールをやめる判断は、そのツールのリポの外にも残骸を作る。**2026-08-27の項目で`rules/global-rules.md` §2の`/cadence`名指しは直したが、**同じ撤去に追随すべき箇所が他にもあった**（上限の内訳・installの実行順・トリアージ分類の`CADENCE`クラス・`--no-codex`の「3リポとも」）。1か所ずつ直すと必ず取りこぼすので、やめるときは`grep -ri <ツール名>`で一括して見る。
- **改善案**: 上流もcadenceを外し、記述を2ブロック構成へ揃える。**過去の経緯（READMEの履歴・本ファイルの過去項目）は事実なので書き換えない**——直すのは「これからの手順」を書いている箇所だけ。
- **反映（2026-08-29）**: 撤去した箇所＝`CLAUDE.md`（2ブロック・方式の対比・コミット本文の例外リスト）／`README.md`（ブロック図・install順・構成表・`--no-codex`の説明）／`rules/global-rules.md` §5.2の例外リスト／`rules/codex-global-rules.md`（トリアージ分類・例外リスト）／`hooks/triage-rubric.txt`と分類フック2本から`CADENCE`クラス／`skills/codex-triage/SKILL.md`。分類はT0 / T1 / T2aの3値になった。⇒ [反映済み → 上記7ファイル]
- **環境側の後片付け（2026-08-29）**: 配置物はcadenceリポのinstallが置いたもので**このリポの管轄外**だが、残す理由が無いのでこのPCでは消した——`~/.claude/CLAUDE.md`と`~/.codex/AGENTS.md`の`cadence-triage`ブロック、`~/.claude/skills/cadence`、`~/.codex/skills/cadence`、`~/.claude/commands/cadence.md`。**常時ロードは14,039 → 13,017B（-1,022B）／AGENTS.mdは9,041 → 8,065B。**最後の1件はquorum側にあった——`quorum/rules/codex-quorum-triage.md`の「T0 / T2a / CADENCEでは…」で、**分類の正本が3値になった以上そちらも直す必要がある**ためquorumリポで直してinstallし直した（quorum `d3aa200`）。**他PCでも同じ後片付けが要る**（installでは消えない。`--no-codex`と同じで「置かない」と「消す」は別）。
- **教訓**: ツールをやめるとき消すべきものは**3層に散る**——①そのツールのリポ ②他ツールのリポに書かれた連携の記述（今回のquorumの1行）③各PCの配置物（ブロック・スキル・コマンド）。①だけ消して終わりにすると②③が黙って残る。

## 2026-08-29 — init-rulesの雛形に個人アカウント名が直書きされている
- **状況**: 配布先（業務リポ）では`skills/init-rules/SKILL.md`から個人アカウント名を落として運用していた。上流は`git config user.email <個人名>@users.noreply.github.com`と`gh repo create <個人org>/<name>`が直書きのまま。
- **気づき**: **このスキルは業務PCへも配布される**ので、雛形に個人アカウント名が入っていると、業務リポの初期化時にそのまま使われかねない（少なくとも、配布先で毎回落とす手間が要る）。**必要な情報ではない**——コミットメールは`gh api user --jq .login`から作れ、作成先のownerは個人かorgかを**その場でユーザーに確認すべき値**であって、スキルが決め打ちしてよい値ではない。もともと「初回はprivate・公開は確認の上で」と慎重に書いてある手順なのに、**ownerだけ黙って決まっていた**のがちぐはぐだった。
- **反映（2026-08-29）**: §2.5のアカウント名を落とし、`gh api user --jq .login` / `gh org list`で候補を出してユーザーに確認する形へ変更。「アカウント名・org名をこのファイルに直書きしない（業務PCへも配布されるため）」を理由つきで明記した。⇒ [反映済み → skills/init-rules/SKILL.md §2.5]

## 2026-08-29 — 「説明で記号を使って省略しない」の本文（受け渡し）
- **状況**: 上の「正本を、配布先のPCから直接編集していた」の未了分。本文は配布先のcommit `8657cd321`（2026-08-28）に残っていたので、そのまま受け取った。取り込みで生成物からは消えていたが、履歴からは取り出せた。
- **判断が要ったところ**: 独立した節にするか、既存の§9「応答の書き方」へ入れるか。**§9に入れた**——受け取った本文の最後の項目が「着手や完了を伝えるときも同じ」で、**逐一報告の決まりを直接受けている**。離すと片方だけ読まれる。見出しを1つ増やさずに済んだぶん、実測は本文1,088Bより小さい +1,023Bで収まった。
- **反映（2026-08-29）**: §9に2つ目の項目として追加。Codex版にも同趣旨を1段落で入れた（例文は1行に畳んだ）。**グローバルCLAUDE.mdは13,017 → 14,040B（残り296B）**、AGENTS.mdは8,065 → 8,851B。⇒ [反映済み → rules/global-rules.md §9・rules/codex-global-rules.md §9]
- **気づき（受け渡しの経路について）**: 今回は「配布先の履歴に残っていた」ので拾えたが、**取り込みで消える経路に本文を置いたこと自体が事故の元**だった。本ファイルができた以上、次からは**本文をIMPROVEMENTS.mdに貼ってから**上流で反映する（この項目がその形の最初の例）。

## 2026-08-31 — PJが「必須の関門」と定めたサブエージェントを、毎回ユーザーへ確認してしまう
- **状況**: 業務のモノレポで、サブタスクを締める前にレビュー役のサブエージェントを通す決まりがある（PJのCLAUDE.mdが複数の関門で必須と明記している）。ところがセッション側の指示に「ユーザーが求めない限りサブエージェントを呼ばない」があり、**PJが必須と定めた呼び出しでも、その都度ユーザーへ確認して手が止まった**。ユーザーからは「許可を出しているつもり」と返ってきた。
- **気づき**: グローバルに、**PJ側の決まりが必須としている呼び出しを、都度の許可なしに実行してよい**という明示が無い。ユーザーが一度許可しても**セッションが変わると同じ確認が再発する**ので、恒久の決まりとして書く場所はグローバルが正しい。fableの「黙って呼ばない」（トリアージ節）は**使用量の監査が目的**で、関門のレビュー役とは目的が違う——同じ扱いにすると、必須の関門が毎回止まる。
- **改善案**: トリアージ節の運用ルールへ1行足す。「**PJのCLAUDE.mdやスキルが「必須の関門」と定めているサブエージェント（レビュー役・検査役）は、都度の許可を取らずに呼ぶ**。呼ぶ前に何を通すかを1行で宣言する。事前に諮るのは、**関門ではない呼び出し**（探索の並列化・調べ物の委譲）と、**fable**（トリアージ節の監査ログが要る）。」
- **補足**: 「必須の関門」かどうかはPJ側の記述で判断できる（必須・恒久と書いてあるか）。書いていない呼び出しは従来どおり諮る。
- **反映（2026-08-31）**: 置き場は**トリアージ節ではなく§3「タスク実行中」**にした——トリアージ節はquorumリポが注入するブロックで、**quorumを入れていないPCでは丸ごと存在しない**（cadence撤去のときと同じ、指す先の無い手順が残る形になる）。この決まりはquorumの有無と無関係に効く必要がある。同じ理由で**fableの例外は書かなかった**（fableの「黙って呼ばない」自体がquorum側の記述で、そこに無いものを例外として挙げても読めない）。Codex版§3にも同文を入れた。上限は14,040 → 14,186B（残り150B）で、置き場を作るために§2の「過去の経緯が必要になった時だけ該当日を遡る」（§1の表の「いつ読む」と重複）と§6のrecall行（「新規記録はNOTES.md / checkpoint」が同節2行目と重複）を畳んだ。⇒ [反映済み → rules/global-rules.md §3・rules/codex-global-rules.md §3]

## 2026-09-01 — 英日境界の半角スペース禁止が、応答とローカルの文書に効いていない
- **状況**: 業務のモノレポで解説スライド（HTML）を直しているとき、ユーザーから「半角文字と全角文字の間に半角スペース空ける挙動がいかにもAIなので、直してもらえますか」と指摘。調べるとPJ側の文体の学びを集めた文書に**2026-07-08から同じ学びが載っていた**（ユーザー明言「英字の後に半角スペースが入るのとか、いかにもAIぽく見えます」）。それでも2か月近く経って同じ指摘を受けた。スライド2本で実際に810箇所あった。
- **気づき①（スコープの穴）**: その学びのスコープが「チャット・課題管理の説明欄・PRの説明・自動応答・手早い共有系」と**外部へ出す文面に限られていた**。**Claudeの応答そのものと、ローカルのMD・スライドが入っていない**。外部公開物の直前だけ公開手順の機械検査で捕まえる作りなので、それ以外の面では出続ける。ユーザーは同じ人なので、どの面で見ても同じだけAI臭く見える——**媒体で分けたのが誤り**だった。
- **気づき②（置き場が2系統に分かれている）**: 応答の文体に効かせるには**出力スタイル**（`~/.claude/output-styles/*.md`）に書く必要があり、ファイルへ書く面に効かせるには**グローバル§9**が要る。この2つが別管理で、片方だけ直すと片方で再発する。出力スタイルはgit管理外の単独ファイルで、**他PCへ配られない**（claude-rulesの配布対象外）。
- **気づき③（上限に余裕が無く、1行のルールすら入らない）**: グローバルは14,186B / 上限14,336Bで**残り150B**（前項2026-08-31の記録どおり）。今回入れたい1行が214Bで入らず、**削り代を探す作業が毎回セットで発生する**。`check-limits.sh`は超過してから知らせるので、**残量が細っていく過程が見えない**。
- **改善案**: ①§9へ1行（「英数字と日本語の間に半角スペースを入れない。書く面すべて」）。②応答へ効かせる分は出力スタイル側へ。**出力スタイルをclaude-rulesの配布対象に含めるか**を検討する（含めないなら、PCごとに手で入れる前提だと明記する）。③`check-limits.sh`に残量表示か、8割で警告する段を足す。
- **補足（この件で踏んだ運用ミス）**: 上の①を入れようとして、**pull専用の`rules/global-rules.md`をこのPCで直接編集して`install.sh`まで走らせた**（`bundled/README.md`に「直接編集しない——次に引いたとき衝突するか、黙って上書きされる」と明記があるのに読み落とした）。気づいて戻したが、**編集もinstallも何にも止められなかった**。生成物側（`~/.claude/CLAUDE.md`）はマーカー行の「手動編集しない」で防いでいるのに、**subtreeとして置いた正本側には同じ注意書きも検査も無い**。改善案 = `rules/*.md`の先頭へ「このファイルは上流が正本。手元のcloneで編集しない」の1行を置く（マーカー行と同じ役目）。あるいは`install.sh`が上流との差分を検出したら警告する。
- **反映①（2026-09-02・ルール本体）**: §9へ「**英数字と日本語の間に半角スペースを入れない**（悪い例「`install.sh`を実行」／良い例「`install.sh`を実行」）。応答・手元のMDやスライド・外部へ出す文面、書く面すべてに効く」をClaude / Codex両方へ入れた。**置き場のバイトは、このファイルの記号ルールにあったコードブロックの例を本文の丸括弧へ畳み、冒頭・§1・§2・§3・§7の言い換え5か所を削り、そして`rules/*.md`自身を新ルールに合わせて作った**（境界の半角スペースがglobal-rules.mdに190か所・codex側に117か所あり、消した分がそのままバイトになる）。結果global-rules.mdは12,002→11,985B、グローバルCLAUDE.mdは14,186→14,167B——**ルールを1本増やして残量が19B増えた**。
- **反映①の副産物（気づき②への答え）**: **ルールを書いている文書自身が190か所その規則を破っていた。**2か月効かなかった理由はスコープの穴だけでなく、規則を載せる面が規則どおりに書かれていなかったことにもある。なお機械置換は一律にはできず、見出し番号（`## 2.`）・番号付きリスト（`3.`）・Markdown記法を列挙している行・「悪い例」側の空白まで潰しかけた。番号の区切りは対象から外し、記法列挙行は除外し、悪い例は手で戻した。
- **反映②（2026-09-02・出力スタイル）**: **配布対象に含めない**と決め、READMEに理由つきで明記した。文章の決まりが2系統に分かれること自体が①の原因なので、`rules/*.md`の§8・§9へ一本化し常時ロードのブロック経由で全PCへ配る。出力スタイルはgit管理外で他PCへ渡らず、このPCには`~/.claude/output-styles/`自体が無い（新規に作れば全PCの応答の組み立て方を変える副作用が大きい）。
- **反映③（2026-09-02・残量）**: `check-limits.sh`が全行に残量を出すようにし、残りが`CR_WARN_MARGIN_BYTES`（既定512B）/ `CR_WARN_MARGIN_LINES`（既定5行）を切ったら`△`を付ける（超過ではないのでexitは0のまま）。**8割ではなく残量marginにした**——グローバルは上限まで使い切る設計で常時98%前後を走るため、割合の警告は鳴りっぱなしになって信号にならない。
- **反映④（2026-09-02・補足の分）**: `install.sh`が**clone以外（配布先へ取り込まれた複製）から走った時に、`rules/*.md`はpull専用だとstderrで知らせる**ようにした（`rules/`に未コミットの変更があればさらに強く警告）。originがclaude-rulesのclone自身なら黙る。改善案にあった「`rules/*.md`の先頭へ1行」は**採らなかった**——その1行は生成物にも注入されて常時ロードのバイトを食い、しかも複製かどうかは文面では判定できない。⇒ [反映済み → rules/global-rules.md §9・rules/codex-global-rules.md §9・tools/check-limits.sh・install.sh・README]
- **残件**: READMEと本ファイルの既存本文は境界の空白をそのままにした。本ファイルは複数PCのcloneから末尾追記でmergeする前提で、全面書き換えは全項目conflictを招く（冒頭の注記と同じ理由）。**新しく書く分から新ルールに従う。**

## 2026-09-14 — 既存PJの`docs/checkpoints/`移行を手でやると、日本語のファイル名で止まり、リンクも取りこぼす
- **状況**: kakeiboで2026-09-12の改訂（checkpointはリポ直下の`checkpoints/YYYY-MM-DD-作業内容.md`）に追従し、17件を移した。`git mv`で改名し、`PROGRESS.md`・PJの`CLAUDE.md`・ADR・checkpoint同士のリンクを書き換える一括スクリプトを、その場で書いた。
- **気づき①（日本語のパス）**: 書き換えの途中でスクリプトが止まった。`git ls-files`は既定（`core.quotepath=true`）で非ASCIIのパスを`"checkpoints/2026-05-03-\347\253\213..."`のように8進エスケープし、引用符で囲んで返す。**ルールがファイル名を日本語にしたので、移行や一括処理を書く全PJで同じ罠を踏む**。`git -c core.quotepath=false ls-files -z`で受ければ避けられる。
- **気づき②（参照の形が1つではない）**: 書き換えるべき参照が3種類あった。リポ直下からの`docs/checkpoints/DATE.md`、checkpoint同士の`](DATE.md)`、そして**階層が1段浅くなることで行き先がずれる相対パス**（checkpointから`../adr/`を指していたものは`../docs/adr/`になる）。検索パターンを1つの形で書くと、残りの形を黙って取りこぼす（kakeiboでは最後の検査で、別の文書に`../adr/`の形が残っているのが見つかった）。
- **気づき③（移行はまだ多く残っている）**: 2026-09-14時点で、`docs/checkpoints/`のままのPJが17ある（voice-inputはADRも12件）。バックログの「開いたPJから順に」のたびに、同じスクリプトを書き直すことになる。
- **改善案**: `tools/`にテスト付きの移行コマンドを置く。日付と作業内容の対応表は人（またはClaude）が決め、コマンドは①`git mv`②見出しの「作業ログ」の差し替え③相対リンクを旧い位置で解決して新しい位置から張り直す④旧パスの残りとリンク切れの検査、までを受け持つ。**作業内容の命名とADRの仕分けは判断が要るので、コマンドにはやらせない**。
- **反映（2026-09-14）**: `tools/migrate-checkpoints.py`（`plan`／`apply`／`check`）とテスト7件（`tools/tests/`）を追加し、READMEの構成に載せた。改善案との違いは2点——`check`は`checkpoints/`内の旧パスを数えない（移行の記録に旧パスが書かれるのは正しい。実物のkakeiboで最初の版が17件を誤検知した）／`install.sh`で配置しない（PJごとに1回きりで、cloneから呼べば足りる）。⇒ [反映済み → tools/migrate-checkpoints.py・README構成]

## 2026-09-15 — migrate-rulesを初めて別PJ（meeting-scribe）へ通すと、手順の穴が6つ出た
- **状況**: meeting-scribeを記録ルールの改訂に揃えた。checkpoint 6件、`docs/adr.md`（16件）、古い形の`REQUIREMENTS.md`、1,445行の`NOTES.md`。スキルの手順どおりに進めた。
- **気づき**: ①`migrate-checkpoints.py check`が日付の無い旧ディレクトリへの言及（READMEの表のリンク、`.gitignore`のコメント）を拾わない ②移設の見出しの書式`## NOTES.mdから移設`が§9の半角スペースの決まりに反する ③ADR番号がコード・READMEから引かれている時の扱いが無い（消すと参照が切れる）。`docs/adr.md`の1ファイル形も想定外 ④手順3が`REQUIREMENTS.md`の新設前提で、既存の古い形（非機能要件・実機の構成・「解決済み」の節）の置き方が無い。雛形の場所も「`init-rules`の`REQUIREMENTS.md`」と、無いファイルを指していた ⑤突き合わせが`NOTES.md`だけで、書き直した`REQUIREMENTS.md`・`PROGRESS.md`に指示が無い ⑥移す節の見出しが他のファイルから「」付きで引かれていないかを見る手順が無い。
- **反映（2026-09-15）**: ①`apply`がディレクトリへのリンクも張り直し、`check`は日付の無い言及をgit管理下のテキストファイルすべてから「確かめる」として出す（exitは変えない。説明として残す文書と、配布先の複製が言及を含むため）。テスト1件追加 ②〜⑥スキルの該当手順に追記。経緯は`checkpoints/2026-09-15-移行スキルの初回適用で出たズレ.md`。⇒ [反映済み → skills/migrate-rules/SKILL.md手順1〜4・7・tools/migrate-checkpoints.py]

## 2026-09-15 — `migrate-checkpoints.py`がモノレポで壊れる（2つの症状）
- **状況**: 業務のモノレポ（1リポの`tasks/<name>/`配下に複数のタスクが並び、各タスクが自分の`docs/checkpoints/`を持つ）へ記録ルールの改訂を取り込んだ。本体とタスク1つを移した。
- **気づき（症状①）**: **applyが日付をキーに全ファイルを書き換えるため、別の場所の同じ日付を指す参照まで書き換わる。**本体の`docs/checkpoints/2026-07-13.md`を移したところ、タスク側が自分の`checkpoints/2026-07-13.md`を指していた参照が本体の新パスへ化けた。**24ファイルが別の文書を指す状態**になり、`git checkout HEAD -- tasks/`で戻して手で直した。移動対象のリポの外は触らない前提の作りだが、モノレポでは「外」が同じリポの中にある。なお**この罠自体は移行先のPJのNOTES.mdに先に記録されていた**（「文字列置換が壊すものは、同じ名前で別の場所を指す参照」）。
- **気づき（症状②）**: **`--repo`にサブディレクトリを渡しても効かない。**applyは内部で`git rev-parse --show-toplevel`を呼んでリポのルートへ解決するので、`tasks/<name>`を指定してもモノレポのルートで動く。**planは指定どおり動く**ので、計画は正しく立つのに適用だけ「対象が無い」で落ちる。planとapplyで解釈が違うのが分かりにくい。
- **改善案**: (a)書き換えの対象を**移動したファイルと同じディレクトリツリーに閉じる**か、置換キーを日付ではなく**移動元パスの完全一致**にする。(b) applyもplanと同じく`--repo`を尊重する（トップレベルへ解決するなら、その旨をエラーで言う）。(c) `check`がモノレポで大量の誤検知（他タスクの`docs/checkpoints/`を「旧パスが残っている」と報告）を出すので、対象ツリー外は黙る。⇒ [反映済み2026-09-15 → tools/migrate-checkpoints.py：`--repo`を「移すPJのディレクトリ」として3コマンドで統一。本文の旧パスは書かれた場所から上へ辿って実在する最も近い`docs/checkpoints/`に属するとみなし、別PJのものは触らない。リンク先は本文置換から外した。checkもPJ外を黙る。モノレポの再現テスト3件を追加。skills/migrate-rules/SKILL.md手順2]

## 2026-09-15 — `migrate-rules`の突き合わせがCLAUDE.mdを対象にしていない
- **状況**: 同じ移行で、手順6「PJのCLAUDE.mdをPJ固有の差分だけに書き直す」を行った。
- **気づき**: 手順7の`check-moved-lines.py`は`REQUIREMENTS.md`と`PROGRESS.md`にしかかけない。**判断で行を消すのは手順6のCLAUDE.mdなのに、そこだけ機械の網が無い。**しかも消す基準が「グローバルと重複する記録ルールの説明」なので、**グローバルと同じ話題で中身が違う行**——上限値の上書き、禁止への格上げ、明示的な例外宣言——が巻き込まれやすい。今回の実例では、上限のPJ上書き・`--force` pushの禁止への格上げ（グローバルは「事前確認」止まり）・規約がsession側の指示より勝つ宣言・rebaseを事前確認の対象外とする宣言・レビューの起こし方の制限（グローバル§3の関門サブエージェントと逆方向）の5件が該当した。
- **改善案**: (a)手順7の突き合わせに`CLAUDE.md`を足す。(b)手順6に「**消す前に、グローバルの既定と違う値・禁止の強さ・例外宣言が含まれていないかを見る**」の1行を足す。(c)手順1のずれの表に「PJのCLAUDE.mdに、グローバルを上書きしている記述があるか」を加えて、移行前にユーザーへ見せる。⇒ [反映済み2026-09-15 → skills/migrate-rules/SKILL.md手順1の表・手順6・手順7（`check-moved-lines.py`を`CLAUDE.md`にもかける）]

## 2026-09-17 — `migrate-checkpoints.py`が「移動済みで名前だけ古い」PJに使えない／移動で外向きの相対リンクが切れる
- **状況**: 業務のモノレポのタスク1つ（checkpoint 37件）を改訂へ揃えた。このタスクは**2026-09-15の一括移行で`docs/checkpoints/`から`checkpoints/`へ移すところまでは済んでおり、名前が`YYYY-MM-DD.md`のまま残っていた**。
- **気づき①（入口が移動に固定されている）**: `plan`も`apply`も`DIR/docs/checkpoints/`を入口にするので、**すでに直下にあるファイルの改名には使えない**。移行は「移動」と「命名」の2つの作業で、**前者だけ済んだ中間状態が実際に生まれる**——一括移行では置き場を揃えるのが先で、命名は1件ずつ見出しを読む判断が要るので後回しになる。今回の33件は手で行った。
- **気づき②（参照の解決を自前で書き直す羽目になる）**: 手でやると、2026-09-15に入った「書かれた場所から上へ辿って実在する最も近い`checkpoints/`に属するとみなす」解決を**もう一度実装することになる**。省くと壊れる——該当する参照は**1,104箇所あったが、書き換えてよかったのは36箇所だけ**で、残りは**同じ日付を持つ他タスクのcheckpoint**だった。一括置換していれば12タスクぶんの記録を別の文書へ向けていた。
- **気づき③（移動で外向きの相対リンクが切れる。2026-09-14の気づき②が未実装）**: 2026-09-15の移動で切れた相対リンクが**2件**見つかった（いずれもタスクの外にある文書を指す形）。`docs/checkpoints/`から`checkpoints/`へ上がると**外を指す相対パスの深さが1段ずれる**。これは2026-09-14の項目の気づき②に「階層が1段浅くなることで行き先がずれる相対パス」として書かれているが、**実装は`docs/checkpoints/`への言及とリンク先だけで、外向きのパスは直していない**。`check`もリンク切れを見ないので、**2日後の移行で気づかれず2週間そのままだった**。
- **改善案**: (a) `plan`／`apply`に**改名だけの経路**を足す（入口を`DIR/checkpoints/`の`YYYY-MM-DD.md`にも広げる）。移動と命名を別々に回せれば、一括移行のあとで1PJずつ命名を進められる。(b) `apply`が**移すファイルの中の外向き相対リンクを、旧い位置で解決して新しい位置から張り直す**（2026-09-14の気づき②の積み残し）。(c) `check`に**リンク切れの検査**を足す（対象ツリーの`](*.md)`が実在するか）。今回は自前で回して2件見つかった。⇒ [反映済み2026-09-17 → tools/migrate-checkpoints.py：(a)入口を移動元で自動判別（`docs/checkpoints/`に日付名があれば移動と改名、無くて`checkpoints/`に`YYYY-MM-DD.md`が残っていれば改名だけ。どちらで動くかを1行目に出し、移動のあとに改名が残る時は件数を告げる）／(c)リンク切れの検査をPJの中の`.md`から張られた相対リンク全部へ広げ、改名前の名前のまま実在しない参照も出す（リポの外を指す行き先は判断しない）。テスト4件追加で計22件。skills/migrate-rules/SKILL.mdの手順2・READMEの構成表]
  - **(b)は既に実装済みだった**（2026-09-14の気づき②は積み残しではない）。使い捨てのリポで確かめると、`apply`は移す本文の外向き相対リンクを旧い位置で解決して張り直す（`../../資料/一覧.md`→`../資料/一覧.md`、`../../../docs/_archive/…`→`../../docs/_archive/…`）。**切れた2件は道具を通さず手で移したぶん**で、`apply`が一度も見ていない。これを拾えるのは(c)の検査だけなので、回帰テストを(b)にも1件置いた。

## 2026-09-18 — 依頼された作業の受け皿が4軸に無く、進行中の作業の現在地が散る
- **状況**: 別PCでの運用から提案が出た。`REQUIREMENTS.md`は「決めたこと」しか持たず、**いま頼まれて動いている作業そのもの**を置く場所がどこにも無い。進み具合は`PROGRESS.md`の数行とcheckpointに分かれ、セッションをまたぐと「何をどこまでやって次に何をするか」を毎回組み立て直すことになる。
- **気づき①（受け皿が無い）**: 作業は「バックログの1行」か「完了の1行」のどちらかにしかならず、**その間の状態に形が無い**。結果、現在地が`PROGRESS.md`へ書かれて計画の二重持ちになるか、checkpointの末尾に埋もれて次回に見つからないかのどちらかになる。
- **気づき②（checkpointを作業単位で串刺しにできない）**: ファイル名が`YYYY-MM-DD-作業内容.md`で、同じ作業の複数日ぶんのログを一括して引く手掛かりが名前に無い。作業名を固定すれば`checkpoints/*-作業名-*`で引ける。
- **改善案**: (a) `REQUIREMENTS.md`に「進行中の作業」節を置き、**1作業1カード**（何をする／叶っている状態／現在地／待ち／作業ログ）で形を固定する。現在地は**上書きし履歴を積まない**。終わったらカードを消して`PROGRESS.md`の完了へ1行＋checkpointリンク。(b)作業を頼まれたらまずカードを立ててから動く（その場で終わる用事は除く）。(c) checkpointを`YYYY-MM-DD-作業名-中身.md`とし、作業名をカードの見出しと同じ語にする。Claude / Codex両方のルールと両方の`init-rules`の雛形へ。
- **反映（2026-09-18）**: 提案どおり4ファイルへ反映。加えて提案に含まれていなかった2箇所——両方の`init-rules`が雛形の説明文で古いファイル名の書式を指していたぶん——も同じ書式へ直した。グローバルは10,204→11,548B（残り2,788B）、Codex側は9,271→10,614B（残り3,722B）。⇒ [反映済み → rules/global-rules.md §1・§2・§3・§4、rules/codex-global-rules.md §1・§2・§3・§4、skills/init-rules/SKILL.md手順3、skills/codex-init-rules/SKILL.md手順3]
  - **積み残し**: `tools/migrate-checkpoints.py`とREADMEの説明は`YYYY-MM-DD-作業内容.md`のまま。改名の対応表は人が埋めるので道具は動くが、**候補名の出し方と文言が古い書式を指している**。既存PJの移行（`migrate-rules`）にもカードの新設が入っていない。`REQUIREMENTS.md`の未決事項に上げた。

## 2026-09-18 — `check-moved-lines.py`の`--from HEAD:<file>`がモノレポで別のファイルを読む
- **状況**: モノレポのタスク1つで`/migrate-rules`の手順7を実行した。`tasks/<name>/`で`python3 check-moved-lines.py --from HEAD:REQUIREMENTS.md ...`を走らせた。
- **気づき**: `HEAD:<file>`はgitのルートからの相対で解決されるので、**リポルート直下の同名ファイルと比べてしまう**。まったく関係ない差分が大量に出て、一見「30行近く落ちた」ように見える。`--from "HEAD:tasks/<name>/REQUIREMENTS.md"`と書けば通る。**SKILL.mdの手順7のコマンド例がPJルート＝gitルートを前提にしている**（`migrate-checkpoints.py`は`--repo`で解決済みなのに、こちらだけ残っていた）。
- **改善案**: (a)手順7のコマンド例を`--from "HEAD:<PJのリポ相対パス>/<file>"`の形にする(b) `check-moved-lines.py`に`--repo`を足し、`--from`のパスをそこから解決する(c)出力の冒頭に「比較元: <解決したパス>」を出す。取り違えは差分を見るまで気づけない。
- **反映（2026-09-18）**: (b)(c)を実装。`REV:PATH`に`./`を付けて`--repo`からの相対として解決し、読んだ場所を`比較元: HEAD:tasks/x/REQUIREMENTS.md`の形で常に出す。**(a)は採らなかった**——(b)が入れば、PJのディレクトリで実行するかぎり既存のコマンド例がそのまま正しく動く。例文をリポ相対に直すと普通のPJで冗長になり、かえって間違えやすい。スキルの手順4・7には「冒頭の比較元を必ず見る」を足した。テスト2件追加。⇒ [反映済み → tools/check-moved-lines.py・skills/migrate-rules/SKILL.md手順4・手順7]

## 2026-09-18 — 改名がPJの外の参照も張り替えるので、PJ配下だけのステージでは漏れる
- **状況**: 同じ移行で`migrate-checkpoints.py apply --repo tasks/<name>`を実行し、`git add -A tasks/<name>`でコミットした。
- **気づき**: **道具はPJの外から改名対象を指すリンクも正しく張り替える**（モノレポのルートの`PROGRESS.md`、別タスクのcheckpointなど3ファイル）。`--repo`はPJの中だけを見ると読めるが、**参照の張り替えはリポ全体に及ぶ**（それが正しい）。PJ配下だけをステージすると張り替えが未コミットで残り、次の`rebase`が「unstaged changes」で止まって初めて気づく。
- **改善案**: (a) SKILL.md手順2の4のコミット手順に「**PJの外の参照も張り替わるので`git status`をリポルートで見てからステージする**」の1行(b) `apply`の最後に、PJの外で変更したファイルを一覧で出す。
- **反映（2026-09-18）**: (a)(b)とも実装。書き換え一覧でPJの外のものに「（PJの外）」を付け、最後に一覧と「リポルートで`git status`を見てからステージする」をstderrへ出す（PJの中だけで済んだ時は黙る）。判定は既存の`ctx.under()`。テスト1件追加。⇒ [反映済み → tools/migrate-checkpoints.py cmd_apply・skills/migrate-rules/SKILL.md手順2]

## 2026-09-18 — 記録の関門で実際に起きたこと（2台の実測。関門についての改善メモはこの1件に集約）

導入初日に2つのセッションで起きた事態。**失ったデータは無い。損なったのは時間と、一度は誤った結論を流したこと。**
先に書いた関門の改善メモ（`git -C`で別のリポを見る／発火しないセッションがある／リポ単位で割れる）は、この項目へ差し替えた。`git -C`の件は独立した実装の誤りとして既に反映済み（`hooks/commit-record-guard.sh`）。

### 起きた事態（時系列）

1. **記録なしのcommitが止まらないまま進んだ。** 序盤のcommitは編集（ヒアドキュメント）と`git add`と`git commit`を1回の呼び出しにまとめていて、関門は一度も発火しなかった。途中で編集とcommitを別の呼び出しに分けたところ発火した。**同じ人・同じセッション・同じリポで、書き方だけで検査されたりされなかったりする。**（両台で発生）
2. **正しいcommitが差し戻された。**worktree内の`NOTES.md`を`git -C <worktree> commit`で入れようとして、本体cloneの状態で判定されて止まった。`git -C`の対応が入った版では起きない。
3. **差し戻しの副作用で、次のcommitが空振りした。** 差し戻しで**コマンド全体が実行されず`git add`も走っていなかった**。通す指定で打ち直したら`no changes added to commit`、3回目でようやく入った。**「差し戻された＝commitだけが止まった」と読むと間違える。**
4. **検証の足場ごと消えて「発火しない」と読み違えた。** 使い捨てリポの作成からcommitまでを1呼び出しで書いたため、関門が発火した時点でディレクトリ自体ができておらず、次のコマンドが存在しないパスを指して判定が`.cwd`へフォールバックした。**関門を調べるコマンドが関門に壊される。**
5. **疎通確認が空振りした。**`check-record-guard.sh`の②を、前にヒアドキュメントを含む呼び出しへまとめて打つと発火しない。単独で打ち直すと発火する。**疎通確認そのものが素通りの条件を踏むと、「効いていない」と誤って報告する。**
6. **誤った結論を2回流した。** 「セッション単位で割れる」「リポ単位で割れる」。どちらも実測に基づくが観察の条件を固定しておらず、もう一方のセッションの検証を2度、誤った方向へ向けた。
7. **スクリプトを直接叩いた結果だけで「効いている」と報告しかけた。** ②が要る理由を導入初日に踏んだ。
8. **`install.sh`が`settings.json`を書き換えることを、実行者が事前に把握していなかった。** 「反映して」の指示で実行し、書き換えは実行後に分かった。

### 原因は独立した2つ（直す順が要る）

**片方だけ直すともう片方が残る。切り出し → 変更なしの扱い、の順。**

**① commitの検出そのものが空振りする。**`head_part=${cmd%%<<*}`は最初の`<<`以降を全部落とすので、commitが後ろにあると検出に入らない。**作業ツリーが汚れていても効く**（ステージ済みで素通りを実測）。同じ`head_part`を疎通確認の印と通す指定の判定にも使うので、**印を後ろに書くと疎通確認が空振りする**（上の5）。
→ `<<`で切るのではなく、**ヒアドキュメントの本体（デリミタからデリミタまで）を取り除いてから**検出する。

**② 検出できたcommitに対する判定が空振りする。**`PreToolUse`なので判定はコマンドの実行前。`git status --porcelain`が空なら`exit 0`。**cleanな状態から`printf … && git add … && git commit`を1呼び出しで打つと、printfもaddもまだ実行されておらず「変更なし」で通る**。`cd "$SP"`（変数）で通った件も、`[ -d "$target" ]`が偽でcwdへフォールバックし、そのcwdがcleanだったため。
→ 変更なしで`exit 0`する条件をやめるか狭める／同じコマンドに`git add`や書き込みのリダイレクトがあれば止める側に倒す／あるいは`PostToolUse`で「直前のcommitに記録が入っていたか」を見る（止められないが**見逃しはなくなる**）。

### いま言えること

**関門が実際に検査できるのは「前の呼び出しまでに変更ができていて、この呼び出しはcommitだけ」という形に限られる。** 対処は、編集の呼び出しとcommitの呼び出しを分けること。

### 案内として足すべきこと

- **疎通確認は単独の呼び出しで打つ**（②の手順に1行）
- **差し戻されたら、その呼び出しで準備した分もやり直す**（関門に限らずBashツール全般で効く）
- **`install.sh`が`settings.json`を書き換える**ことを実行前に告げる

### 残る課題

**「登録した＝効いている」と思える形になっていない。** 上の8件のうち3件（1・7・6）はこの一点から出ている。関門を入れること自体より、**効いていることを確かめ続けられる形**の方が難しい。

## 2026-09-25 — ループのひな型：1工程が止まるとループ全体が異常停止になる、ほか
- **状況**: 別PCで`templates/loop`（`397b2d7`時点）を読み、開発用のひな型を一時リポへ入れて動かした。付属のテストはすべて通るが、テストが通っていない経路で次の問題を再現した。
- **気づき①（重い）: 工程が1つ止まると、後ろの工程が「未完了」として催促され続け、3回で実行が落ちる。** 開発用は工程が一本道なので、要件の工程を`block`すると`next`は空になる。それでも設計・実装・テストは「未着手」として未完了に数えられ、Stopフックが「続けてください」を出し続ける。上限の3回に達すると「状態が進まなかったため止めました」で実行を止める。統括役の指示にある「残りがすべて止まっていれば止まってよい」が成り立たない。既存のテスト「残りが全部人待ちなら止まってよい」は、1工程だけの実行（`--only`）で確かめているため、この経路を通らない。
  - **改善案**: 止まっている工程に（間接的にも）依存する未着手の工程は、催促の対象から外す。`status`にも「前提の○○が止まっているため待ち」と出す。止まった工程と無関係な工程（`after`で切り離したもの）は、これまでどおり催促する。
- **気づき②（重い）: ゲートで差し戻したときの理由が、いつも同じ定型文になる。** 差し戻しの理由はゲートの出力の最後の行から取っているが、その行は常に共通部品が出す「ゲート不合格: <工程>（✖の項目を直してください）」になる。何が足りなかったか（✖の行）が状態の記録に残らず、統括役が記録から理由を拾って工程役へ渡す流れが空になる。
  - **改善案**: ✖の付いた行を集めて差し戻しの理由にする（無ければ最後の行）。共通部品の冒頭コメント「最後の1行が差し戻しの理由として工程役へ渡る」も合わせて直す。
- **気づき③: テストのゲートが「失敗」という語だけで不合格になる。** テスト報告に`失敗|FAIL|未実施`が1つでもあれば落とすので、「失敗 0件」と書いた健全な報告も不合格になる。
  - **改善案**: 語ではなく、結果の列で判定する。テストの工程役に「結果の列は`合格`・`不合格`・`未実施`のどれかだけを書く」と決めさせ、ゲートは要件idを含む表の行の結果が`合格`かだけを見る。表そのものが無ければ不合格にする。
- **気づき④: ゲート設計の考え方と、開発用の設定が食い違っている。** `GATES.md`は「変更されたファイルが許された範囲の中か」を決定論に置くべきものとして挙げているのに、開発用の`pipeline.json`は同じ中身の問い（実装が設計の範囲に収まるか）を判断役へ回している。
  - **改善案**: 設計の工程に「触ってよいファイルの一覧」を機械で読める形で書かせ、実行を始めた時点のコミットを状態に記録する。実装のゲートで、差分の変更ファイルとその一覧を突き合わせる。ひな型の約束ごとが増えるので、入れるかどうかは判断が要る。
- **気づき⑤（軽い）**:
  - 止まっている工程や、提出済み（レビュー待ち・ゲート待ち・判断待ち）の工程にも`start`できてしまい、`block`や差し戻しの回数を素通りする。`unblock` / `reopen`へ案内して拒むのがよい。
  - Stopフックが実行を止めた理由（`halted`）は、`resume`しても消えない。`status`にも表示されない。
  - `decide`は判断の記録をidの前方一致で探すので、工程idが前方一致する別の工程（`test`と`test-e2e`など）の記録にも正解を付けてしまう。記録にある実行・工程の項目で突き合わせるのがよい。
  - SubagentStopフックは、ループが動いていない時でも、同じ名前の工程役に報告の形を求める。実行中（一時停止中を含む）だけに絞るのがよい。
- **優先**: ①と②は仕組みの前提（止まってよい止まり方・差し戻しで理由を渡す）を崩すので、どこかのリポへ導入する前に直したい。④は約束ごとの追加なので、別に判断する。

## 2026-09-25 — 全自動パイプラインの失敗事例2件（別PC）と、ループのひな型に足りない歯止め
- **状況**: 別PC（業務リポ）で、ループのひな型を過去の問いの精査に入れた。入れる前に、同じPCで過去に全自動を試して畳んだ2件の記録を読み直した。cadenceの振り返りの材料として、実物の失敗事例をここに残す。

### 事例1: 工程とゲートを備えた全自動パイプラインが、仕組み自身の保守で埋まった（2026-07-13〜08-14）
- **構成**: 入力は1件1枚の契約書（完了の定義・受け入れ条件・変更してよい範囲）。設計 → 実装 → PR → 反映の検知 → 動作確認 → 記録の6工程で、3か所にゲートを置いた。ゲートはテスト・lintの終了コードを先に見て、その後に複数モデルの合議（quorum）と反証役2体で判定した。人の出番は「起票」と「外への副作用の直前」の2つまで減らし、2時間ごとにcronで起動していた。
- **起きたこと**: 約6週間で400件超を起票した。畳む時点で生きていた96件のうち**約3分の2が、仕組み自身を守るための作業**だった（停止の抜け道を塞ぐ検査・記入漏れの検出・免除の経路・鮮度の確認など）。業務に効くのは約4分の1だけ。検査と試験のコードは約10万行まで増えた。象徴的な回として、設計レビューに30万トークンを使って実装は0行、指摘の大半は「仕組みが複雑すぎる」だった。ゲートに7回落ちた作業が1件あり、実測の機会は0回だった。
- **止まらなかった理由**:
  1. **レビューのたびに、指摘が次の作業として自動で起票された。** 1件を止めても、残りが新しい作業として必ず生まれる。
  2. **判断役に「そもそも作るべきか」を問う口が無かった。** ゲートは「指摘が出る限り不合格」で、検査・測定器・歯止めを作る作業では指摘が原理的に尽きない。
  3. **上限が1件の中にしか無かった。** 1件あたりのゲート回数の上限はあったが、全体の件数・時間・トークンの上限が無かった。
  4. **人を外しすぎた。** 完了は承認不要で、「知らせて続行」が既定。利用者は途中の裁定を1分も読まず、実質お任せになっていた。
  5. **範囲の縛りがファイル単位だけだった。** 許された範囲の内側で方向がずれても、機械では捕まえられない。
  6. **完了の条件が「ゲートを通ったこと」だった。** 全ゲートとE2Eを通った後に方向違いで全損した回があり、起動して応答するだけで合格にした回もあった。
  7. **周期起動が、ホストのメモリが枯渇した時期と重なった。**
- **畳み方**: 96件を棚上げし、67件を一括で中止した。未審査の実装1,020行を取り消し、退避物を約12MB移した。さらに3回（08-25・09-07・09-18）に分けて、実行ログ2.7GB・止まっていたPR 6本・ブランチ43本・監視フックと無人エージェント5本を片付けた。**欠陥を1つずつ直すのではなく、欠陥が存在しうる構造をやめた。**

### 事例2: 1問ずつ回す無人ループが、止まってばかりだった（2026-08-29〜09-14）
- **構成**: 1回の呼び出しで10〜15周。1周は「1問の精査 → 再チェック」。止まる条件は、人の判断待ちが5件たまる・同じ型の指摘が3回続く・夜間停止が近い、の3つ。
- **起きたこと**: 約9回回したが、どの回も1〜10周で止まった。同じレビュー指摘は手当てを3通り変えても次の周で再発した。直しは溜め場へ積まれて、出口が無いまま96件に膨らんだ。一晩で5〜16件が起票された。自動でPRを作る別の仕組みは、裁定済みの記述を理由なく覆した。**利用者は「量より1問の確からしさ」を取り、無人運転をやめた。**

### ひな型と突き合わせて
- **既に効いているもの**:
  - 確信度の閾値と抜き取り（人の正解が溜まり続ける）
  - 工程ごとの差し戻しの上限
  - 止まった工程の後ろを催促しない（事例2の「判断待ち5件で全体が止まる」への答えになっている）
  - 統括役が合否を宣言しない
- **足りない歯止め（改善案）**:
  1. **起票権を持たないことを明文化する。** 統括役・レビュー役・判断役のどれも、工程や作業を増やさない。気づいたことは「候補」の置き場へ1行置くだけにし、起票は人がする。事例1の直接の原因は自動起票だった。
  2. **実行全体の上限を持つ。** 件数（扱う項目数・PR数）と時間の上限を置き、`time_budget_sec`は目安でなく打ち切りにする選択肢を用意する。`max_rework`と`max_auto_continues`は1工程・1停止の単位にしか効かない。**上限で止まることを、わざと当てて先に確かめる**（cadenceの学び「打ち切りの経路は実走で発火しない」と同じ）。
  3. **ルールの昇格に、失効の条件を入れる。** 判断役の結論を決定論ゲートへ昇格する仕組みは、事例1の「検査が増え続ける」と同じ方向に働きうる。昇格したルールにも、一定回数発火しなければ外す候補にする失効の条件と、ゲート本数の上限か「1本足したら1本見直す」を持たせる。cadenceの「一度も所見を生んでいない観点は降格の候補」と同じ考え方。
  4. **GOAL.mdの雛形で、完了を「利用者から見た効果の測定」と書かせる。** ゲートを通った・検査が緑・Readyは完了に数えない、と一行入れる。仕組み自身を良くする作業は完了に数えない。
  5. **判断役の選択肢に「作らない・消す」を入れる。** 直す案と消す案があれば消す案を先に検討する、と指示に入れる。確信度が低いときは作らない側へ倒す。
  6. **書いてよい面を工程役に絞る。** 事例1で増えたのは、検査・歯止め・測定器といった仕組みの部品だった。ループの中では、それらを新設・改修しない既定にし、要るときは`blocked`で人へ返す。
  7. **「止まらない」と「人に聞く」がぶつかる場面を先に決める。** 決めていないと「止まらない」が勝つ。外への副作用の直前と、要件が満たせないと分かったときは必ず止める。常駐指示（早止まり対策）と同じ面に、この例外を並べて書く。
- **別PCでの入れ方**: 上の1・4・5・6と、全体の上限（1回の実行で扱う項目数と時間）は、ひな型を入れた先で手で足した。2・3・7はひな型側で持つほうが、入れる先ごとに書き直さずに済む。
- **先出し（使いにくさ4つ）**: ⇒ 2026-09-25「ループのひな型を実務で1日回した結果」の1・9・11・12へ統合済み
  - 状態を工程の単位で持つので、1問ずつ独立に止めたいときは、問いごとの工程の列を生成する必要があった
  - フックの登録先のsettings.jsonが、git管理外のリポジトリがある
  - 判断役の既定のhaikuでは、業務の裁定を読み込む判断に力が足りない
  - 工程役とレビュー役の名前が汎用なので、1つのリポジトリにループを2つ置けない

## 2026-09-25 — ループのひな型を実務で1日回した結果（別PC）：効いた歯止めと、ひな型に足りない口
- **状況**: 別PC（業務リポ）で、汎用のひな型を過去の問い合わせの精査に入れ、1日回した。1回目は「集める → 仕分け」（82件を仕分け）、2回目は1件ずつ「決める → 測る → 直す（PR） → 記録」の4工程を10件ぶん。判断役は既定の閾値より高く0.95で始め、「少しでも迷ったら人へ」とした。

### 効いたもの（残す）
- **実行全体の時間の上限（打ち切り）**: 6時間で`loopctl`が実行を止めた。統括役が止まるのではなく、道具が止めた。導入時に分担の数の上限を当てて止まるのを確かめてあった。
- **レビュー役の差し戻し**: 3回、止めるべきところで止めた。
  - 仕分けで、読む表も数も違う2件を統合していた
  - 決めたことの中で2つの決まりが食い違っていた
  - 直しの工程役が、1件の問いのために全体に効く規則を書き換え、既存の行に禁止を重ねていた（全体の失敗事例と同じ「直しが広がる」形）
- **止まった工程の後ろだけが待つ**: 1件が人待ちでも、ほかは進んだ。
- **判断役の誤りの例（lessons）**: 人の線引きを2回で覚えた。一方で、覚えた線引きを広く読みすぎる回も出た（下の3）。

### ひな型に足りない口（改善案）
1. **1件ずつ独立に回す形を、ひな型が持っていない。** 状態は工程の単位なので、項目ごとに工程の列を生成する道具を入れた先で書いた。しかも`pipeline.json`はループ自身のファイルなので、生成し直すには実行を閉じて新しく始める必要がある。
   - **改善案**: `pipeline.json`に「項目の一覧 × 工程の型」を書けるようにし、`begin`で展開する。または`loopctl`に項目を足す口を持たせる。
2. **工程役の指示が食い違っている。** 汎用の工程役の定義に、次の2つが並んでいる。
   - 「気づきは`.claude/loop/candidates.md`へ1行足す」
   - 「`.claude/loop/`を書き換えない」

   工程役は後者を取って気づきを書かず、報告に書いて統括役へ回していた。改修の検査の対象からは`candidates.md`が外れているので、実害は無いが迷う。
   - **改善案**: 後者に「`candidates.md`を除く」と書く。
3. **判断役の問いの文面が、人の裁定の後で読み方が割れた。** 「過去の裁定から一意に導けるか（新しい裁定を要しないか）」を、人の裁定で閉じた項目について、判断役はyesともnoとも読んだ。人の答えは「人の裁定も根拠に含める」だったので、2件が誤りとして記録され、較正の材料が汚れた。
   - **改善案**: GATES.mdの問いの書き方に「問いは成果物のいまの状態について聞く。経緯（人に聞いたかどうか）を問わない」を足す。
4. **人の答えの一部しか、学習の材料にならない。** `decide`で記録されるのは、判断役の問いへの正解だけ。工程役が「人に聞くこと」で止めた項目への人の答え（今日は10件中7件がこの形）は、`judgments.jsonl`に入らず、判断役は学べない。
   - **改善案**: `block`に選択肢と推奨を持たせ、人の選んだ答えを`judgments`に残す口を足す。
5. **人に選んでもらう形の決まりが無い。** 統括役は推奨を会話でだけ示した。人が「全部推奨どおり」と答えると、どの案を選んだかがファイルからは辿れない（レビュー役が2回指摘した）。
   - **改善案**: 4の`block`の選択肢と推奨を状態に残せば、そのまま解ける。
6. **人待ちをまとめて見て答える口が無い。** 閾値0.95・正解0件から始めると、ほぼ全件が人に回る（今日は10件すべてが一度は人待ちに並んだ）。統括役は毎回、表を手で組んで出していた。
   - **改善案**: `loopctl.py pending`で、人待ちの問いを選択肢・推奨・判断役の答えと一緒に一覧で出す。`decide`はまとめて打てるようにする。
7. **時間の上限で止まった後の再開の手順が無い。** 予算は開始からの経過で数えるので、そのまま`resume`すると即座にまた止まる。再開するには`pipeline.json`の予算を手で上げ、`accept-self`する必要があった。
   - **改善案**: `resume --extend <秒>`を足すか、止まっていた時間を予算から除く。
8. **「必ず止まる」に共有ブランチへのpushが入っている。** 記録をpushするのが既定の手順のリポでは、入れた先で例外を書き足す必要があった。
   - **改善案**: 止まる場面の一覧を、`pipeline.json`で入れ替えられるようにする。
9. **フックの登録先がgit管理外のことがある。** 入れた先では`.claude/settings.json`がgit管理外で、フックは入れたworktreeにしか無かった。実行中の状態も同じ。
   - **改善案**: 導入の道具が、登録先がgit管理外であることを検出して知らせる。READMEに「そのworktreeから起動する。消さない」を足す。
10. **統括役として起動しなかったセッションでも回せたが、早止まり対策は効かなかった。** 利用者が普通のセッションで「回して」と言い、そのセッションが統括役の手順で回した。区切りごとに人へ報告して止まったので、その都度、人の返事で次へ進んだ。
    - **改善案**: READMEの回し方に「`--agent`で起動しないとStopフックは効かない」を1行足す。
11. **判断役の既定モデルが、業務の裁定を読む判断には弱い。** 入れた先でsonnetへ替えた。
    - **改善案**: 判断役の定義に「問いが文書の読み込みを要するなら、モデルを上げる」を1行足す。
12. **汎用の工程役とレビュー役の名前が固定で、1つのリポに2つ目のループを置けない。**
    - **改善案**: 導入の道具に、名前の頭を付ける指定を足す。

### ひな型の外（配布しているフック）
- **記録の関門（後追いのフック）が「記録なし: <理由>」の行を認めない。** コミットの本文に`記録なし: …`を入れても、`PostToolUse`の監査が「記録が入っていない」と止めた（2回）。pushの関門は通ったので、止めたのは後追いの監査だけ。
  - **改善案**: 後追いの監査も、pushの関門と同じくこの行を例外として読む。

### 優先
- 2・3・9は小さく、すぐ直せる。
- 4〜6は、人を減らしていく仕組みの本体で、「判断役を育てる」が回るかどうかを決める。
- **反映（2026-09-25）**: 2・3・9・10・11と、後追いの監査の件を反映。2は工程役5つの「書き換えない」に`candidates.md`へ1行足すのは除くと添えた（改修の検査`SELF_FILES`はもともと対象外）。3はGATES.mdの問いの書き方へ。9は導入の道具が`settings.json`・統括役の定義・`pipeline.json`のどれかがgitの無視対象なら知らせる（新規・`--update`の両方）。11はREADMEの導入手順とGATES.mdの表に、文書の読み込みを要する問いならsonnetへ。後追いの監査はpushの関門と同じ正規表現で本文の`記録なし:`を読む。テスト2件追加。⇒ [反映済み → templates/loop/*/agents/{dev-*,step-worker}.md・templates/loop/common/.claude/loop/{GATES,README}.md・tools/loop-scaffold.py・hooks/commit-record-audit.sh]
- **反映（2026-09-25・残り）**: 1・4〜8・12も反映。1は`per_item`（工程の型）を`begin`で`<項目>/<工程>`へ展開し、**項目の一覧は`pipeline.json`でなく実行の状態に持つ**（`add-item`で足してもループ自身が変わらない）。判断役の問いのidとルールは型の単位で育つ。4〜6は`block --ask`（問い・選択肢・推奨・判断役の答え）を`judgments.jsonl`へ残し、`pending`で一覧、`answer`でまとめて答える。人の答えは`lessons.md`の「人の裁定」に載り、較正で閾値が出た問いは判断役の答えで止めずに進む。7は`resume --extend <秒>`。8は`pipeline.json`の`must_stop`。12は`loop-scaffold.py --name <名前>`（置き場`.claude/loop-<名前>/`・エージェント`<名前>-…`・参照とフックの付け替え・`--update`にも対応）。テスト16件追加。⇒ [反映済み → templates/loop/common/.claude/loop/bin/loopctl.py・stop-guard.py・gates/lib.sh・README.md、templates/loop/*/.claude/agents/*.md、templates/loop/*/.claude/loop/pipeline.json、tools/loop-scaffold.py、README.md]

## 2026-09-26 — ループのひな型を個人の開発PJへ入れた時の引っかかり（初回、実行前）
- **状況**: 個人の開発PJ（Python・pytest・ruff）へ汎用版を入れ、「項目＝実装する対象、項目ごとに実装→コミット、全項目の後に結合テスト」の形に設定した。まだ実行は回していない。設定と空打ちで気づいた分。
- **気づき①: ひな型のPythonが、入れた先のリンタで大量に落ちる。** 入れた先のリンタ設定（行長・1行複数文の禁止など）で、状態を動かす道具だけで176件出た。工程役に「リンタを通す」と指示すると毎回落ちるので、入れた先でリンタの対象から`.claude`を外した。
  - **改善案**: 導入の道具が、入れた先のリンタ設定（pyproject・eslint等）を見つけたら「`.claude`を対象から外す」ことを知らせる。または、ひな型のPythonを一般的なリンタ設定で通る書式にする。
- **気づき②: 項目を直列に回す口が無い。** どの項目も同じコア部分を触りうるので並列にすると衝突するが、`next`は全項目の最初の工程を並べて返す。統括役の定義に「1件ずつ」と文章で書き足して塞いだ（機械では守られない）。
  - **改善案**: `per_item`に`"serial": true`を持たせ、`next`が未完了の項目のうち一覧で最も上のものだけを返す。
- **気づき③: 項目の一覧を`begin`へ渡す経路が、統括役の定義に無い。** 統括役は`begin --goal`だけを打つので、そのままだと項目0件で始まる。統括役の定義に`--items-file <パス>`を書き足した。
  - **改善案**: `per_item.items_file`を`pipeline.json`に書けるようにし、`begin`が既定で読む（一覧の中身は実行の状態に持つ、という今の設計はそのまま）。
- **気づき④: 汎用版にコミットの工程が無く、記録の関門との噛み合わせを各リポで書くことになる。** 「1作業1コミット」と記録の関門（編集とcommitを同じ呼び出しに入れると止まる・記録ファイルを含めないと止まる）に合わせ、コミットだけの工程と、未コミットの変更が無い・push済みかを見るゲートを入れた先で書いた。
  - **改善案**: 汎用版に「コミット」の工程の型とゲート（作業ツリーが綺麗・pushの要否はPJの方針）を用意し、工程役への指示に「記録を書く → 別の呼び出しでcommit」を入れておく。
- **気づき⑤: 触ってはいけない場所を禁じる部品が無い。** 範囲の検査は設計書の「触ってよいファイル」節が前提で、汎用版では使いにくい。自動生成のデータ・凍結した資産を「触ったら不合格」にする検査を入れた先で書いた（開始コミットからの差分＋未追跡ファイル）。
  - **改善案**: `lib.sh`に`forbid_paths <正規表現>`を足す。
- **気づき⑥: 導入後の手順0（上限で止まるか）の確かめが、ループ自身のファイルの書き換えを伴う。** `pipeline.json`の上限を1にして`begin`し、確かめたら戻して`finish`する。閉じた実行の状態に「作業中」の工程が残り、次に`status`を見た時に紛らわしい。
  - **改善案**: `begin --limit max_shards=1`のように実行の間だけ上限を差し替える口か、`loopctl.py selftest`を用意する。
- **気づき⑦: `--update`が、ひな型側に変更の無いファイルまで「手で直されているので触っていない」と差分コマンド付きで並べる。** 開くと差分は空。
  - **改善案**: 入れた時の版と今の版でひな型側が変わっていないファイルは「手を入れてある（ひな型側の変更なし）」と1行にまとめる。
- **気づき⑧: 初回のお試しで、ひな型そのものの課題を貯める口が無い。** `candidates.md`は作業の別件と混ざる。実行の終わりに「差し戻しの回数と理由・催促の回数・人待ちの件数・ゲートの誤判定」を数える振り返りも、統括役が手で書くしかない。入れた先では統括役の定義に「`[ひな型]`付きで`candidates.md`へ1行、止まる前に振り返りを1ブロック」を書き足した。
  - **改善案**: `loopctl.py retro`で実行の数字（項目ごとの経過・差し戻しの理由の分布・催促の回数・人待ち）を出す。`candidates.md`に作業の別件とループの課題の2節を最初から置く。
- **ひな型の外（配布しているフック）**: 記録の関門の入口のフックは、コミットの本文の`記録なし: <理由>`を読まず、環境変数を付けないと通らない。後追いの監査とpushの関門はこの行を読むので、記録の要らないcommitには行と環境変数の両方が要る。
  - **改善案**: 入口のフックも`-m` / `-F`の本文から`記録なし:`を読む（読めない形なら従来どおり環境変数）。
- **優先**: ②④は項目ごとに回す使い方の前提で、次にどこかへ入れる前に欲しい。①③⑤⑦は小さい。⑥⑧はお試しの段階で効く。
- **反映（2026-09-26）**: 全件反映。①導入の道具が入れた先のリンタ設定（ruff・flake8・eslint・biome）を見つけ、`.claude`を外していなければ外し方を知らせる（新規・`--update`の両方。ひな型の書式は変えていない）。②`per_item.serial`＝各項目の最初の工程が前の項目の最後の工程を前提にする（`next`も`start`も順番を守らせる）。③`per_item.items_file`を`begin`が既定で読み、項目0件では始めない（`--allow-empty`で明示）。統括役の定義は一覧の渡し方を`begin`より前に置いた。④共通の`gates/commit.sh`（未コミットの変更が無い・`PUSH_REQUIRED=1`ならpush済み）と汎用版の`commit`工程、工程役へ「記録を書く呼び出しとcommitの呼び出しを分ける」。⑤`lib.sh`に`forbid_paths <正規表現> [説明]`（開始コミットからの差分＋未追跡、ループ自身と開始時の変更は除く）と`need_clean_tree`・`need_pushed`。⑥`begin --limit <名前>=<値>`（その実行の間だけ。`status`に出る。次の`begin`へ持ち越さない）、README手順0をこれに。⑦`--update`で、ひな型側が変わっていない手直しは1行にまとめる。⑧`loopctl.py retro`（経過・項目ごとの経過と差し戻し・差し戻しの理由の分布・催促の回数・人待ち・判断役の誤り）とStopフックの催促の累計、`candidates.md`を「作業の別件／ループの仕組みの課題（[ひな型]）／振り返り」の3節に、統括役は止まる前に`retro`を貼る。フック: 記録の関門の入口も、commitのメッセージ（`-m`・`-F -`のヒアドキュメント・`-F <ファイル>`）の行頭の`記録なし:`で通す（同じ呼び出しで書く別のファイルの中身では通さない）。テストを追加（ひな型・導入の道具・記録の関門で計32件）。⇒ [反映済み → templates/loop/common/.claude/loop/{bin/loopctl.py,bin/stop-guard.py,gates/lib.sh,gates/commit.sh,gates/commands.env,README.md,GATES.md,candidates.md}・templates/loop/common/.claude/agents/loop-conductor.md・templates/loop/generic/.claude/{loop/pipeline.json,agents/step-worker.md}・tools/loop-scaffold.py・hooks/commit-record-guard.sh・README.md]

## 2026-09-26 — ループのひな型：「まず人に仰いで判断役を育てる」使い方に、既定の設定が合わない
- **状況**: 上の項目と同じ個人の開発PJ。利用者の求める流れは「最初は止まらないことより人に判断を仰ぐ → 人の答えで判断役を育てる → 熟練したら判断役から決定論ゲートへ上げる」。ひな型は部品（`judgments.jsonl`・較正・`lessons.md`・ルールの影の検証と`promote`）をすべて持っていたが、既定の設定が「最初から任せる」寄りだった。
- **気づき①: 較正前の閾値（`default_threshold` 0.9）で、人の答えが0件のうちから判断役の答えが自動で通る。** 判断役は自己申告の確信度を高めに出しがちなので、最初の数件から人を素通りしうる。人の答えが溜まらず、較正の材料も増えない。入れた先では1.01にして「較正前は必ず人へ」にした。
  - **改善案**: 較正前の既定を「人へ回す」にする（`default_threshold: null`＝較正で閾値が出た問いだけ任せる）。今の動きが要るリポは数値を書く。READMEの「判断役を育てる」に、段階ごとの設定（育てる段階・任せる段階）を並べる。
- **気づき②: 人待ちの項目を飛ばして次へ進むと、止まった項目の未コミットの変更を次の項目が巻き込む。** 項目ごとに回す形は、項目が独立している前提。1つの作業ツリーで順に作る使い方では、止まった項目の変更が次の項目のcommitに混ざる。入れた先では`per_item.serial`（各項目の最初の工程が前の項目の最後の工程を前提にする）を展開の関数に3行で足し、前が止まれば後ろも待ちになるようにした。止まった後の表示（「前提の○○が止まっているため待ち」）とStopフックが催促しないことは、既存の仕組みがそのまま効いた。
  - **改善案**: `per_item.serial`をひな型に入れる（前の項目の気づき②と同じ口）。または、項目が止まった時に作業ツリーの変更を退避する手順を統括役の定義に持たせる。
- **気づき③: 人に仰ぐ段階の統括役の振る舞いが定義に無い。** 止まる時に`pending`を貼る・`calibrate`の数字を見せるまではあるが、「判断役を通さずに人へ聞かない」「人に答えの理由を書いてもらう（`lessons.md`の材料になる）」「任せる範囲を広げる（`calibrate --apply`）・決定論へ上げる（`promote`）は人」が段階の運用として書かれていない。入れた先で統括役の定義に節を足した。
  - **改善案**: 統括役の定義に「育てる段階」の節を持たせ、`pipeline.json`の設定（例: `judge.stage: "train" | "delegate"`）で切り替える。
- **反映（2026-09-26）**: ①`judge.default_threshold`の既定をnull（較正で閾値が出た問いだけ任せ、それまでは全部人へ）にし、ひな型の2つの工程表もnullに。工程役の問い（`block --ask`）は元から較正済みの問いだけ任せる作りで、判断役の問いだけが食い違っていた。最初から任せたいリポは数値を書く。READMEの「判断役を育てる」に段階（育てる／任せる／決定論へ上げる）ごとの起きることと人がすることの表。②は前の項目の反映で`per_item.serial`として入っていた。③統括役の定義に「判断役を育てる」節（判断役を通さずに人へ聞かない・答えの理由を`--note`で頼む・止まる時に`calibrate`の一致率を見せる・`calibrate --apply`と`promote`は人）。**`judge.stage`の切り替えは入れなかった**——段階は較正の記録で決まり、設定を足すと「任せる段階なのに較正が無い」ような食い違いの組み合わせが生まれるため。テスト2件追加（較正前は確信度が高くても人へ・較正で閾値が出た問いは任せる）。⇒ [反映済み → templates/loop/common/.claude/loop/{bin/loopctl.py,README.md}・templates/loop/common/.claude/agents/loop-conductor.md・templates/loop/{dev,generic}/.claude/loop/pipeline.json]

## 2026-09-26 — fix-spacing.pyが共通ルールのブロックの中まで直した
- **状況**: 別PCでのPJの移行（共通ルールをAGENTS.mdへ書き込んだ後）で、`fix-spacing.py --write AGENTS.md`を当てた。
- **気づき**: 共通ルールのブロック（`claude-rules:embed:begin`〜`end`）の中まで直した。マーカー行の「版 <commit>」が「版<commit>」になり、`embed-rules.py`が前回の選択を読めず「初回は--optionsを」と止まった。§9の悪い例「`install.sh` を実行」も良い例に書き換わった。そのPJでは同じ選択でブロックを作り直して元に戻した。
- **改善案**: `fix-spacing.py`がembedマーカーの間を既定で守る（`--keep`の既定に入れる）。あわせて`migrate-rules`の手順7に「AGENTS.mdはブロックの外だけを対象にする」と一行足す。
- **反映（2026-09-26）**: 提案どおり。`fix-spacing.py`は共通ルールのブロック（マーカー行を含む）を常に飛ばす（`--keep`の既定でなく、外す指定も持たない。生成物なので直すなら正本側）。`migrate-rules`の手順7に「`AGENTS.md`はブロックの外だけが対象」。加えて、壊れた後でも戻せるよう`embed-rules.py`がマーカー行の境目の空白が詰められていても版と選択を読むようにした（書き込み直すと元の形に戻る）。テスト2件追加。⇒ [反映済み → tools/fix-spacing.py・tools/embed-rules.py・skills/migrate-rules/SKILL.md]

## 2026-09-26 — 境目の空白の補正が、名前で引かれている見出しと突き合わせを壊した
- **状況**: 別PCで既存PJに`migrate-rules`を当てた。手順7で`fix-spacing.py`を当てた。
- **気づき**: `NOTES.md`の見出しが詰められ、他のファイルが名前で引いている見出し（`NOTES.md「…」`の形で5つ）と`[[...]]`の行き先がずれた。さらに空白の補正のあとで`check-moved-lines.py`を回すと、補正した行がすべて「どこにも無い」と出て、突き合わせが使えなくなった。
- **改善案**: ①`migrate-rules`の手順7で、突き合わせ（`check-moved-lines`）を空白の補正より先に行う順序にする。②`fix-spacing.py`は見出し行を「判断が要る候補」に回すか、`NOTES.md「…」`と`[[…]]`で引かれている見出しを守る。③`check-moved-lines.py`に、空白を無視して比べる指定（例`--ignore-space`）を足す。
- **反映（2026-09-26）**: 3つとも。①`migrate-rules`の手順7を「先に突き合わせ、あとで空白を補正する」順に並べ替えた（後から回すなら`--ignore-space`）。②`fix-spacing.py`は見出しの行を直さず「判断が要る候補」に回し、`[[…]]`とファイル名の直後の`「…」`の中身を触らない（両方とった。見出しを守るだけだと、参照の側だけ直って逆向きにずれる）。直すなら見出しと参照を`grep`で一緒に揃えると手順に書いた。③`check-moved-lines.py --ignore-space`。あわせて手順7の上限の判定のパスを`.claude-rules/check-limits.sh`へ（グローバル廃止の追従漏れ）。テスト3件追加。⇒ [反映済み → skills/migrate-rules/SKILL.md・tools/fix-spacing.py・tools/check-moved-lines.py・README.md]

## 2026-09-26 — migrate-checkpoints.pyの3つの引っかかり（モノレポのタスク移行）
- **状況**: 別PCのモノレポで、タスクごとにcheckpointを移した。
- **気づき①**: 日付だけの名前（`YYYY-MM-DD.md`）しか扱わず、「日付＋英語名」（`2026-07-16-llm-judge.md`の形）の42件と「日付_日本語」の1件は「日付名でないので残した」になった。
  - **改善案**: 名前の表で旧ファイル名を直接指定できると、同じ道具で移せる。
- **気づき②**: インラインコードの中の例示リンク`` `[text](url)` ``を、実在しない相対パスとして`../docs/checkpoints/url`へ張り替えた。
  - **改善案**: コードスパンの中は触らない、または実在しない先は張り替えない。
- **気づき③**: symlink（`NOTES-shared.md`が隣のPJの`NOTES.md`を指すもの）の中のリンクを、symlink側の位置で解決して「リンクが切れている」と出した。
  - **改善案**: 実体のパスで判定する。
- **反映（2026-09-26）**: 3つとも。①対応表の1列目に旧ファイル名（拡張子なし・日付で始まるもの）を書けるようにした。移す時は`plan`が「日付＋別の名前」も並べて候補に日付の後ろを入れ、改名だけの時は表に書いた分だけ改名する。移した後の名前が重なれば止める。②リンクの張り替えと検査はコードフェンスとインラインコードの中を見ない。加えて、旧い位置で解決した行き先が元々無いリンクは張り替えない（両方とった）。③symlinkのファイルは`apply`で書き換えない（実体の別PJのファイルを書き換えてしまうため）。`check`は旧パスの言及を見ず、リンク切れだけを実体の位置で解決して見る。`migrate-rules`の手順2に使い方を書いた。テスト4件追加。⇒ [反映済み → tools/migrate-checkpoints.py・skills/migrate-rules/SKILL.md]

## 2026-09-26 — init-rulesの雛形に共通ルールと合わない箇所が2点
- **状況**: 別PCで、モノレポのタスク作成スキルを共通ルールに合わせて直していて気づいた。
- **気づき①**: `PROGRESS.md`の雛形に「現在のフェーズ」の節がある。放っておくと完了の一覧になる形で、共通ルール§2の「いまの状態だけ」と合わない。`migrate-rules`の手順5は「現在地／次の一手／進行中・ブロッカー／完了」を指定しており、雛形と食い違っている。
- **気づき②**: 最初のcheckpointの名前が`<YYYY-MM-DD>-初期構成.md`で、同じ節の注記にある`YYYY-MM-DD-作業名-中身.md`の形になっていない。
- **改善案**: `PROGRESS.md`の雛形を「現在地 / 次の一手 (Top 3) / 進行中 / 完了」にする。最初のcheckpointを`<YYYY-MM-DD>-<作業名>-初期構成.md`にし、作業名を手順2で確かめる。
- **反映（2026-09-26）**: 提案どおり。`init-rules`の`PROGRESS.md`の雛形を「現在地 / 次の一手 (Top 3) / 進行中 / 完了」にし、各節に書き方の注記（現在地は上書きして経緯を積まない・進行中はカードを指すだけ・完了は1行＋リンク）を添えた。最初のcheckpointを`<YYYY-MM-DD>-<作業名>-初期構成.md`にし、手順2で最初の作業名（既定「立ち上げ」）を確かめる。このリポ自身の`PROGRESS.md`も「現在のフェーズ」に経緯が積もっていたので「現在地」に直した。⇒ [反映済み → skills/init-rules/SKILL.md・PROGRESS.md]
