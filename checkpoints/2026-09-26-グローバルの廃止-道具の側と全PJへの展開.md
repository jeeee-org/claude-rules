# 2026-09-26 グローバルの廃止 — 道具の側と全PJへの展開

## 依頼と決定
- 共通ルールをグローバルに入れるのをやめる。二重管理はAIで回す（ユーザーの判断）。
- 業務・共有リポにも書き込んでコミットする。自分のPJは個人の運用を全部入れる。既存の`CLAUDE.md`は`AGENTS.md`へ移して統一する。

## 道具の側（claude-rules）
- `rules/common-rules.md`からグローバル向けの条件（`if:global`）を外し、PJ書き込みの版だけにした。生成物`rules/global-rules.md`・`rules/codex-global-rules.md`は削除。
- `tools/build-rules.py`は本文を作るだけの道具へ（`--variant` / `--options`）。`OPTIONS`と`parse_options`の正本もここ。
- `tools/embed-rules.py`に2つ足した。
  - `--absorb-claude-md`：`CLAUDE.md`のPJ固有の指示をブロックの下へ移して`CLAUDE.md`を消す。グローバル時代の決まった言い回しを置き換え、機械で判断できない行を行番号付きで出す。
  - `--scan DIR`：配下のPJの状態（最新・古い・未書き込み・`CLAUDE.md`あり）を一覧にする。
- `install.sh`は共通ルールを入れず、以前のブロックを`~/.claude/CLAUDE.md`・`~/.codex/AGENTS.md`から取り除く（控え`.bak`）。移行途中の逃げ道として`--keep-global-rules`。仮の設定ディレクトリで、除去・前後の中身の保持・残す指定を確かめた。
- スキル：`install-rules`を書き直した（1つのPJ／まとめて／道具の導入）。`init-rules`と`codex-init-rules`は`AGENTS.md`へ共通ルールを書き込んでから雛形を作る形へ。`migrate-rules`は冒頭に読み替えの注記。
- フックの文面の「グローバル§」を「共通ルール§」へ。README・`tools/collect-state.sh`も追従。
- このリポ自身も`CLAUDE.md`を`AGENTS.md`へ統一した（内容はグローバル廃止に合わせて書き直してから移した）。
- テスト：版の作り方を全版・全選択で見る形へ書き換え、移して統一・状態の一覧のテストを追加。

## 全PJへの展開（このPC）
- `~/Develop`の23リポ（claude-rules含む）すべてに`--options all --absorb-claude-md`で書き込んだ。書き込み前に全リポの作業ツリーが綺麗なことを確認した。
- 機械で置き換えなかった行を手で直した。
  - ai-tech-diaryの「CLAUDE.mdに含めるべきでない」等3行 → AGENTS.md
  - asset-managerの表の行 → `AGENTS.md`
  - incorporation-2026の「グローバルに従う」 → 「共通ルールに従う」
  - task-managerの「CLAUDE.md / CLAUDE.local.md」 → 「AGENTS.md / CLAUDE.local.md」
  - kakeiboの「### AGENTS.md」（Next.jsの注意書きの説明）→「### Next.jsの注意書き（AGENTS.md末尾）」（元の見出しは、統一後はファイル全体を指してしまうため）
  - interview-copilot / meeting-scribe / voice-inputの「グローバルに置くと」（環境変数の話）はそのまま
- PJ固有の部分の上限はすべて6,144B以内。kakeiboは6,096Bで残り48B（元のCLAUDE.md 5.7KBにNext.jsの注意書きが加わった）。
- 各リポで1コミット。記録の関門は`CR_SKIP_RECORD_GUARD=1`で通し、pushの関門にはトレーラ`記録なし: 共通ルールの配布の変更`を付けた（PJの作業ではなく配布の変更で、経緯はここにある）。リモートのある20リポはpush。disc・myhomeはリモート無しでcommitまで。
- `CLAUDE.local.md`があるPJ（6つ）も、Claude Codeが`AGENTS.md`を読むことを実測で確かめた（NOTES）。
- コードのコメントに「CLAUDE.mdの事故防止ルール」等の参照が残るリポがある（interview-copilot・voice-input・meeting-scribe）。中身はAGENTS.mdへ移っており、コードの変更は範囲外として触っていない。
- 最後に`./install.sh`で`~/.claude/CLAUDE.md`・`~/.codex/AGENTS.md`からブロックを外した（どちらも0Bになった。控えは`.bak`）。`--scan`で23PJすべて「最新」。

## 残したこと
- 他PC：作業するPJの`git pull` → まだのPJ（業務・共有リポ）へ書き込み → `./install.sh`。

## 追補：PJ側に要らない説明を削った
- 両方向けの冒頭の1文「PJのルールはこの`AGENTS.md`に統一する（Claude CodeもCodexも直接読む）。`CLAUDE.md`は作らず、既にあるなら先頭の`@AGENTS.md`で繋ぐ（…）」は、claude-rules側の設計の説明で、各PJのコンテキストには要らない（ユーザーの指摘）。
- PJで作業するAIが壊しうるのは「`CLAUDE.md`を作る」ことだけなので、`PJに`CLAUDE.md`を作らない（あるとClaude Codeがこのファイルを読まなくなる）。`の1行だけ残した。経緯と挙動の詳細は`NOTES.md`「配布の仕組み」にある。
- 全PJを書き込み直した（前回の選択のまま）。
- 同時に1行足した：`~/.claude/CLAUDE.md`・`~/.codex/AGENTS.md`に以前の共通ルールのブロックが残っていたら、ユーザーに伝えて了承を得て消す（ユーザーの提案）。以前のclaude-rulesを入れた人がPJだけ受け取った時、`install.sh`を知らなくてもPJのAIが二重読み込みに気づけるように。グローバルのファイルは常時読み込まれるので、AIは中身を見て判断できる。
- 全23PJを書き込み直し、各PJで1コミット（リモートのある20PJはpush）。`--scan`で全PJ最新。
- さらに本文を全行見直し、経緯・設計の説明を3か所削った：冒頭の「PJへ書き込んだもの」、§2の「ヘッダが数千バイトに膨れるのはこれが原因」（過去の事故）、§5の「機微はprivate/publicで管理する前提」。決まりの短い理由（〜するため・〜だから）は、書かれていない場面の判断材料になるので残した（ユーザー了承）。
- 全23PJを書き込み直した（20PJはpush、disc・myhomeはcommitまで）。

## 追補：AGENTS.mdの外に残る言い回し（mtg-practiceのセッションからの共有）
- mtg-practiceのセッションが、ループの検査スクリプト（`.claude/loop/gates/commit.sh`）のコメントに「グローバル§5」が残っていたのを直した（あわせて`AGENTS.md`の余分な空行。元の`CLAUDE.md`にあった空行で、道具の不具合ではない）。
- `--absorb-claude-md`は`AGENTS.md`しか直さない。他のPJを`git grep`で見たところ、過去の記録（checkpoint）とquorumのREADMEの経緯の段落以外には残っていなかった。
- 一方、**claude-rules自身が各PJへ配る`.claude-rules/check-limits.sh`のコメントに「グローバル §2」「PJ の CLAUDE.md で上書き可」が残っていた**。`tools/check-limits.sh`・`install.sh`・`tools/collect-state.sh`・`tools/migrate-checkpoints.py`のコメントを「共通ルール§N」「AGENTS.md」へ直し、全PJの複製を更新した。
- `install-rules`スキルとREADMEの他PC向け手順に、「`AGENTS.md`の外に残る言い回しを`git grep`で探して直す（過去の記録は直さない）」を足した。
