# 2026-09-15 移行スキルの初回適用で出たズレ

`migrate-rules`をmeeting-scribeへ初めて通した（meeting-scribe側の記録は`checkpoints/2026-09-15-共通ルールの取り込み.md`）。手順どおりに進めて出たズレ6件を、ユーザーの承認を得てスキルと移行コマンドに反映した。

## 出たズレと直し方

1. **`migrate-checkpoints.py check`が旧ディレクトリへの言及を拾わなかった**
   - meeting-scribeでは、READMEの「記録の置き場」の表にあった`[docs/checkpoints/](docs/checkpoints/)`と、`.gitignore`のコメント2か所が残った。`check`は日付入りのパスしか見ておらず、対象も`.md`と`.json`だけだった。
   - 直し方：`apply`がディレクトリそのものへのリンクも張り直す。`check`は日付の無い言及を、git管理下のテキストファイルすべて（`checkpoints/`の外）から「確かめる」として出す。**exitは変えない**——方式の説明として旧ディレクトリ名を残す文書もあり、配布先に取り込んだ複製（スキルやツール自身）も言及を含むため、失敗にすると毎回止まる。テストを1件足した（14件すべて成功）。
2. **移設の見出し`## NOTES.md から移設`が§9の半角スペースの決まりに反していた**。スキル自身の手順7の検査に掛かる。`## NOTES.mdから移設`に直した。
3. **ADR番号がコードから引かれている場合の扱いが無かった**。meeting-scribeでは`proto/*/src/main.rs`やREADMEが`ADR-011`などを引いており、`docs/adr.md`を消すと参照が切れる。方針の行末に（ADR-NNN）を残す、覆ったADRは改めた側の行に残す、を足した。ADRが`docs/adr.md`の1ファイルにまとまっている形も、手順1の表に足した。
4. **手順3が`REQUIREMENTS.md`を新しく作る前提だった**。meeting-scribeには古い形の`REQUIREMENTS.md`があり、非機能要件・実機の構成・「解決済み」の節をどこへ置くかの指針が無かった。雛形の4節に組み替え、PJ固有の節は「要求・方針」の下へ小見出しで置く、「解決済み」は片付いた未決として移行のcheckpointへ、を足した。「`init-rules`の`REQUIREMENTS.md`」はファイルではないので「`init-rules`の`SKILL.md`にある雛形」に直した。
5. **突き合わせが`NOTES.md`にしか指示されていなかった**。meeting-scribeでは`REQUIREMENTS.md`・`PROGRESS.md`を書き直した分にも使い、書き直しで消えた行（片付いた未決・旧バックログ）を1行ずつ確かめられた。手順7に2本のコマンドを足した。
6. **移す節の見出しが他のファイルから「」付きで引かれていないかを見る手順が無かった**。meeting-scribeでは`proto/dual-capture/README.md`が`NOTES.md「Windows側の環境構築でハマった点」`を引いていた（残す側だったので切れなかった）。手順4に探し方を足した。

## 確かめたこと

- `python3 -m unittest discover -s tools/tests`：14件すべて成功（新規1件）。
- 直した`check`をmeeting-scribeの移行前の状態で流すと、README・`.gitignore`の言及が出ることは、足したテストの形（同じ2種類の言及）で固定した。

## 追記：voice-inputの移行で出た7件目（突き合わせコマンドの字下げ）

- 同じ日にvoice-inputも`migrate-rules`で移行した。旧`REQUIREMENTS.md`の未決事項の本文（箇条書きの続きで、行頭に空白2つ）を、字下げを外して`NOTES.md`の節へ移したところ、`check-moved-lines.py`が3行を「どこにも無い」と出した。中身は移し先にあった。
- 原因：比較の前に行末の空白しか落としていなかった。箇条書きの続きの行は、移す先で字下げが変わるのが普通なので、移し漏れでない行が混ざって一覧が読みにくくなる。
- 直し方：行頭の空白も落として比べる。テストを1件足した（15件すべて成功）。直した後にvoice-inputの旧`REQUIREMENTS.md`を突き合わせ直すと、36行が33行になり、その3行は出なくなった。
- スキルの手順は変えていない。
