# 2026-09-19 体裁の統一 — PJの`CLAUDE.md`の境目の半角スペース

このPJの`CLAUDE.md`に、英数字と日本語の間の半角スペース（§9違反）が17行に残っていた。
同じ日に足した2行はスペース無しで書いたので、1つのファイルの中で書き方が混ざっていた。全部落として揃えた。

## やり方

行頭のマーカー（`- ` `## ` 等）を切り分けてから、英数字側と日本語側の境目のスペースだけを消す置換をかけた。
**機械にかけると2か所壊れる**ので、diffを1行ずつ見て直した。

- **記号の周りのスペースまで落ちる**。`commit + 自動 push`が`commit +自動push`になった（`+`を英数字側に数えたため）。全角の`＋`へ直して`commit＋自動push`。
- **日本語どうしの間の余分なスペースは残る**（`記録ルールは グローバル`）。これは§9とは別の打ち間違いなので、目で見て落とした。

## 確かめ方と、その網の粗さ

`grep -nE '[0-9A-Za-z`*)\]] +[ぁ-んァ-ヶ一-龥]|[ぁ-んァ-ヶ一-龥] +[0-9A-Za-z`*(\[~§]'`で残存ゼロを確認。
ただし**この網は過検出する**。ほかのファイルへ当てると、次のものが引っかかる——直す前に中身を見る。

- 規則の**悪い例そのもの**（`rules/global-rules.md`の「「`install.sh` を実行」でなく」）
- 見出しの番号（`### 5.2 コミットメッセージ規約`）、`次の一手 (Top 3)`のような括弧
- `NOTES.md`の日付プレフィックス（`- 2026-09-12 全体圧縮のやり方：…`）——この形は意図した区切り
- 太字の閉じ`**`の直後のスペース（隣は英数字ではないので§9の対象外）

`rules/*.md`は、この4種を除くと**実質的に残存なし**だった。

## 判定

PJの`CLAUDE.md`は3,361B → 3,308B / 6,144B（残り2,836B）。グローバルの2ファイルは無関係。

## 残っているもの

`README.md`と`skills/init-rules/SKILL.md`には、同じ境目のスペースがまだ多い（上の網で49件・39件。過検出を含む）。今回は触っていない。

## 追記: 雛形（`init-rules`）と隣の2スキルも揃えた（同日）

雛形は**これから作られる全PJの`CLAUDE.md`等に複写される**ので、ここを直すと元から揃う。
`skills/init-rules/SKILL.md`・`skills/codex-init-rules/SKILL.md`・`skills/migrate-rules/SKILL.md`の3本。

置換の規則を作り直して、スクラッチパッドのスクリプトに固定した（使い捨て。リポには置いていない）。
1度目の素朴な置換で**4種類の壊し方**をしたのが、規則を作り直した理由。

1. **行頭の番号が潰れた**。`### 1. 現状確認` → `### 1.現状確認`。見出しの`###`だけをマーカーとして切り分けていて、その後ろの`1. `を本文とみなしたため。マーカーの切り出しに「見出し記号のあとの番号」を足した。
2. **記号の周りが潰れた**。`4軸 + checkpoint`・`# 要件 / スコープ`・`## やること / バックログ`が`4軸+checkpoint`・`# 要件/スコープ`になった。§9が言う「英数字と日本語の間」ではなく**記号を挟んだ両側**なのに、`+`と`/`を英数字側に数えていた。`' + '`と`' / '`は**そのまま残す語**として保護した——とくに`やること / バックログ`は**節の名前**で、`migrate-rules`が同じ字面で指している。
3. **太字の閉じの直後まで詰めた**。`確認する。** 切れていたら`が`確認する。**切れていたら`に。ここは`**`を透かして見れば日本語どうしなので対象外。判定を「**印（`*`と`` ` ``）を透かした先が英数字かどうか**」に変えた。コード印を透かすのは必要（`` `install.sh` を実行``は直す）なので、透かす向きは同じで、**見る文字だけを英数字に限った**のが直し。
4. **チェックボックスが潰れた**。`- [ ] （ここに）`が`- [ ]（ここに）`に。マーカーの選択肢で`- `が先に当たり、`[ ] `が本文に落ちていた。長い方を先に並べた。

手で直したのは3か所。`Conventional Commitsか 日本語要約1行 か。`（英数字側だけ詰まって残りが浮いた→読点で書き直し）、
`記録ルールは グローバル`（日本語どうしの余分なスペース）、`雛形＋ `.gitignore``（全角プラスの後ろ）。

`<YYYY-MM-DD> `（`PROGRESS.md`の完了行と`NOTES.md`の日付プレフィックス）と`次の一手 (Top 3)`も保護した。
最後に、印を透かす判定で3本を再走査して残存ゼロを確認（引っかかるのは番号と保護した語だけ）。

## 追記: READMEと、印を挟んだ取りこぼし（同日）

`README.md`（48行）に同じ規則をかけた。表・コードブロック・URLは壊れていない
（表の`|`とコード印の内側は、判定に使う文字が英数字でも日本語でもないので触られない）。

かけた後の見直しで、**印（`**`と`` ` ``）を挟んだ境目に取りこぼし**が3種類見つかった。判定を「印を透かす」形にはしていたが、
**透かすのが片側だけ**だった。

1. `…ではない。** PCごとに` — 左が日本語・右が英数字で、**間に太字の閉じがある**。右側の印しか透かしていなかった。
2. `分類子プロセスは `--ephemeral …`` — コード印の**中身が記号で始まる**（`--`）ので、英数字を探す判定に当たらなかった。
3. `各雛形の `<...>` プレースホルダ` — 中身に英数字が1文字も無い。閉じのコード印の直後は無条件に詰める規則を足して解決。

3種類を足して5ファイル（README・PJの`CLAUDE.md`・スキル3本）を再走査し、残存ゼロ。

**ついでに`rules/global-rules.md`を1か所直した**（`…は使わない。** recallされても`）。
§9を載せている正本自身が§9を破っていた例で、[NOTES](../NOTES.md)の2026-09-02の学びと同じ形。
正本の2ファイルで残ったのは、**規則の悪い例そのもの**（「`install.sh` を実行」でなく…）と見出しの番号だけ。
`~/.claude/CLAUDE.md`は12,227B → 12,226B。

## 追記: 残りのファイルも揃えた（同日・これで全部）

`PROGRESS.md`・`NOTES.md`・`REQUIREMENTS.md`・`IMPROVEMENTS.md`・`skills/codex-triage/SKILL.md`。
`IMPROVEMENTS.md`は89行と最も多かった（他PCからの追記が積まれた分）。

**保護をもう1つ足した**——行頭の**日付プレフィックス**（`- 2026-09-12 全体圧縮のやり方：…`、`- [x] 2026-09-18 …`）。
`NOTES.md`の項目と`PROGRESS.md`の完了行がこの形で、詰めると**日付と本文が地続きになって読めなくなる**。
行頭マーカーの切り出しに`\d{4}-\d{2}-\d{2}\s+`を足して解決した。

手で戻したのは1か所。`IMPROVEMENTS.md`の`` `## 9. 応答の書き方` ``——**引用している見出しの名前**で、
行頭ではなくコード印の中にあるので、行頭マーカーの保護が効かなかった。**引用された番号付き見出しは詰めない。**

全12ファイルを再走査して、残るのは次の3種だけになった。どれも意図したもの。

- 規則の**悪い例そのもの**（`rules/*.md`の「「`install.sh` を実行」でなく」）
- 見出し・箇条書きの番号（`### 5.2 コミットメッセージ規約`）
- 引用された見出しの名前と、保護した語（`' + '`・`' / '`・`次の一手 (Top 3)`・日付プレフィックス）

`IMPROVEMENTS.md`は他PCからの追記でconflictの種になり得るが、**揃える方を採った**（ユーザーの判断）。
次に他PCが追記する時、末尾追記なら衝突しない。途中行を触っていれば衝突する。
