# 他PCへの反映 — 手順とひな型の更新（2026-09-25）

## 依頼
別PCでも反映させるための手順を書いておく。

## 洗い出し
他PCでやることは3系統: ①このリポの更新（`git pull`と`./install.sh`と再起動）②途中の混乱への手当て（to-doの設定の消し忘れ・`IMPROVEMENTS.md`の衝突）③**ループのひな型を入れたリポの更新**。③は導入スクリプトが既存ファイルを上書きしないので、pullしても入った先に届かない。業務のPCは既に1つ入れている。

## やったこと
- 手順をシェルで書き、`397b2d7`の版で入れたリポを作って通した。最初の検査（道具が手で直されていないか）が全部「直されている」と誤判定——zshが`$REV:templates`の`:t`を修飾子と解釈していた。**利用者のシェルで壊れる手順を文書に書くより、道具にする**と判断。
- `tools/loop-scaffold.py --update`を追加。`.claude/loop/.scaffold.json`の版（入れた時の版）をcloneのgit履歴から引いてファイルごとに突き合わせ、無いものは作る・入れた時のままなら上書き・手で直したものは触らず`git diff <入れた時の版> <今の版> -- …`を添えて一覧に出す。`.scaffold.json`の版を更新する。`--dry-run`可。テスト用に隠しオプション`--old-dir`（古い版のtemplates/loop）。新規に入れる時は`--profile`必須のまま。
- 実物の確認: `397b2d7`で入れて`lib.sh`だけ手で直したリポに`--update`を当て、作成2・上書き20・手で直した1（`lib.sh`、差分コマンド付き）を確認。
- READMEに「他のPCへ反映する」節（取り込む・確かめる・pullが止まった時・ひな型を入れたリポを上げる、と日付ごとの一回限りの手当て）。構成表の`loop-scaffold.py`の行に`--update`。PROGRESSの次の一手1をこの節への参照に置き換えた。

## 確かめたこと
- テスト186件合格（新規4件: 手を入れていないものは新しく・直したものは触らない／確認だけなら変えない／入れた記録が無ければ断る／新規はprofile必須）。
